# راهنمای تست سیستم داشبوردها

## ✅ پاسخ سوال شما

**بله، صفحه‌ای که می‌بینید همان صفحه لیست داشبوردها است!** این صفحه باید نمایش داده شود.

---

## 🔍 بررسی اینکه از معماری جدید استفاده می‌کند

### روش 1: تست از طریق مرورگر (با دامنه واقعی)

1. **وارد سیستم شوید:**
   ```
   https://bi.cfu.ac.ir
   ```

2. **بعد از لاگین SSO:**
   - باید به صفحه لیست داشبوردها redirect شوید
   - این همان صفحه‌ای است که در تصویر می‌بینید

3. **بررسی معماری جدید:**
   - روی یکی از داشبوردها کلیک کنید (مثلاً "آمار کلی اعضای هیات علمی")
   - بررسی کنید که:
     - URL باید `/dashboards/d1` باشد
     - اگر کاربر مرکزی هستید، باید فیلترها نمایش داده شوند
     - داده‌ها باید بر اساس سطح دسترسی شما فیلتر شده باشند

### روش 2: تست محلی با Mock SSO

برای تست در `localhost` بدون نیاز به SSO واقعی:

1. **اجرای سرور در حالت DEBUG:**
   ```bash
   cd app
   python app.py
   ```

2. **استفاده از Mock SSO:**
   ```
   http://localhost:5000/mock_login?username=test_central&access_level=central_org
   http://localhost:5000/mock_login?username=test_province&access_level=province_university&province_code=1
   http://localhost:5000/mock_login?username=test_faculty&access_level=faculty&province_code=1&faculty_code=1001
   ```

3. **بعد از mock login:**
   - باید به `/dashboards` redirect شوید
   - صفحه لیست داشبوردها نمایش داده می‌شود

---

## 📋 چک‌لیست تست

### ✅ صفحه لیست داشبوردها:
- [ ] صفحه لیست داشبوردها نمایش داده می‌شود
- [ ] لیست داشبوردها کامل است (7 داشبورد)
- [ ] جستجو کار می‌کند

### ✅ داشبوردها:
- [ ] روی داشبورد کلیک می‌کنید و داده نمایش داده می‌شود
- [ ] URL درست است (`/dashboards/d1`)
- [ ] خطای JavaScript وجود ندارد

### ✅ فیلترها (برای کاربران مرکزی):
- [ ] فیلتر استان نمایش داده می‌شود
- [ ] فیلتر دانشکده نمایش داده می‌شود
- [ ] فیلترها کار می‌کنند

### ✅ دسترسی:
- [ ] کاربران مرکزی همه داده‌ها را می‌بینند
- [ ] کاربران استان فقط داده‌های استان خود را می‌بینند
- [ ] کاربران دانشکده فقط داده‌های دانشکده خود را می‌بینند

---

## 🔧 تست سریع

### تست 1: بررسی Route
1. در Developer Tools (F12) > Network tab
2. روی داشبورد کلیک کنید
3. بررسی کنید که request به `/dashboards/d1` انجام می‌شود
4. Response status باید 200 باشد

### تست 2: بررسی فیلترها
1. اگر کاربر مرکزی هستید
2. به داشبورد بروید
3. باید فیلترها را ببینید (در بالای صفحه)

### تست 3: بررسی داده‌ها
1. به داشبورد بروید
2. داده‌ها را بررسی کنید
3. باید بر اساس سطح دسترسی فیلتر شده باشند

---

## 🐛 عیب‌یابی

### مشکل: فیلترها نمایش داده نمی‌شوند
**راه‌حل:**
1. بررسی کنید که سطح دسترسی کاربر `central_org` یا `admin` است
2. بررسی کنید که `user_context` به template پاس داده می‌شود
3. بررسی کنید که template `_filters.html` در داشبورد include شده است

### مشکل: داده‌ها فیلتر نمی‌شوند
**راه‌حل:**
1. بررسی کنید که `province_code`, `faculty_code` در دیتابیس تنظیم شده
2. بررسی کنید که `UserContext` به درستی ایجاد می‌شود
3. لاگ‌های سرور را بررسی کنید

---

## 💡 نکات مهم

1. **برای تست محلی:** از Mock SSO استفاده کنید (`/mock_login`)
2. **برای تست واقعی:** از دامنه واقعی استفاده کنید (`https://bi.cfu.ac.ir`)
3. **بررسی معماری:** مطمئن شوید که از معماری جدید استفاده می‌شود
4. **لاگ‌ها:** لاگ‌های سرور را بررسی کنید

---

## 🎯 نتیجه

**بله، صفحه‌ای که می‌بینید همان صفحه مد نظر است!** 

برای اطمینان از اینکه از معماری جدید استفاده می‌کند:
1. روی یکی از داشبوردها کلیک کنید
2. بررسی کنید که فیلترها نمایش داده می‌شوند (برای کاربران مرکزی)
3. بررسی کنید که داده‌ها بر اساس سطح دسترسی فیلتر می‌شوند

اگر فیلترها نمایش داده نمی‌شوند یا داده‌ها فیلتر نمی‌شوند، به من اطلاع دهید تا بررسی کنیم.


