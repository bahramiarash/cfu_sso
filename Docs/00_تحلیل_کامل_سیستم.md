# مستندات تحلیل کامل سیستم BI دانشگاه

## 📋 فهرست مطالب

1. [مقدمه](#مقدمه)
2. [تحلیل نیازمندی‌ها](#تحلیل-نیازمندی‌ها)
3. [تحلیل ذینفعان](#تحلیل-ذینفعان)
4. [تحلیل فرآیندهای کسب‌وکار](#تحلیل-فرآیندهای-کسب‌وکار)
5. [تحلیل داده‌ها](#تحلیل-داده‌ها)
6. [تحلیل امنیت](#تحلیل-امنیت)
7. [تحلیل عملکرد](#تحلیل-عملکرد)
8. [تحلیل قابلیت‌های سیستم](#تحلیل-قابلیت‌های-سیستم)

---

## مقدمه

### هدف از این مستند

این مستند به عنوان مرجع کامل برای درک و بازسازی سیستم BI دانشگاه تهیه شده است. با مطالعه این مستند، یک توسعه‌دهنده یا هوش مصنوعی می‌تواند دقیقاً همین سیستم را بازسازی کند.

### دامنه پروژه

سیستم BI دانشگاه یک پلتفرم جامع برای:
- نمایش و تحلیل داده‌های دانشگاهی
- مدیریت دسترسی چندسطحی
- همگام‌سازی خودکار داده‌ها از منابع مختلف
- مدیریت پروژه و وظایف
- گزارش‌گیری و داشبوردهای تحلیلی

### محدودیت‌ها و پیش‌فرض‌ها

- سیستم از SSO مرکزی دانشگاه استفاده می‌کند
- داده‌ها از API Gateway دریافت می‌شوند
- سیستم برای استفاده در محیط داخلی دانشگاه طراحی شده است
- پشتیبانی از زبان فارسی و تقویم شمسی الزامی است

---

## تحلیل نیازمندی‌ها

### نیازمندی‌های عملکردی

#### 1. احراز هویت و مدیریت دسترسی

**نیازمندی‌ها:**
- ورود کاربران از طریق SSO مرکزی
- مدیریت Session و خروج امن
- کنترل دسترسی بر اساس سطح سازمانی
- پشتیبانی از سطوح دسترسی: مرکزی، استان، دانشکده، ادمین

**جزییات پیاده‌سازی:**
- استفاده از OAuth2 برای SSO
- Flask-Login برای مدیریت Session
- جدول `users` برای اطلاعات کاربران
- جدول `access_levels` برای سطوح دسترسی
- جدول `dashboard_access` برای دسترسی به داشبوردها

#### 2. سیستم داشبوردها

**نیازمندی‌ها:**
- نمایش داشبوردهای مختلف بر اساس دسترسی کاربر
- فیلترینگ داده‌ها بر اساس سطح دسترسی
- Cache برای بهبود عملکرد
- پشتیبانی از نمودارها و نقشه‌ها

**داشبوردهای موجود:**
1. **داشبورد آمار دانشکده‌ها (d1)**
   - نمایش آمار کلی دانشکده‌ها
   - فیلتر بر اساس استان/دانشگاه/دانشکده
   
2. **نقشه دانشکده‌ها (d2)**
   - نمایش جغرافیایی دانشکده‌ها روی نقشه ایران
   - استفاده از Shapefile استان‌ها
   
3. **نقشه پردیس‌ها (d3)**
   - نمایش پردیس‌ها روی نقشه
   
4. **نسبت دانشجو به استاد (d7)**
   - محاسبه و نمایش نسبت
   
5. **مانیتورینگ LMS (d8)**
   - نمایش داده‌های زنده LMS
   - نمودارهای زمانی
   - هشدار برای مقادیر بالا

6. **داشبورد دانشجویان**
   - آمار و اطلاعات دانشجویان

**معماری داشبوردها:**
- کلاس پایه `BaseDashboard`
- Registry Pattern برای مدیریت داشبوردها
- Data Providers برای دریافت داده
- Visualization Components برای نمایش

#### 3. پنل مدیریت

**نیازمندی‌ها:**
- مدیریت کاربران (ایجاد، ویرایش، حذف)
- مدیریت دسترسی به داشبوردها
- مدیریت همگام‌سازی داده‌ها
- مشاهده لاگ‌های دسترسی
- تنظیمات داشبوردها

**قابلیت‌ها:**
- فقط کاربران با سطح دسترسی Admin دسترسی دارند
- امکان تعریف فیلترهای دسترسی برای هر کاربر
- امکان تعریف محدودیت زمانی برای دسترسی

#### 4. مدیریت پروژه و وظایف

**نیازمندی‌ها:**
- ایجاد و مدیریت پروژه‌ها
- سیستم Kanban برای مدیریت وظایف
- اختصاص کاربران به ستون‌ها و تسک‌ها
- سیستم برچسب‌گذاری
- گزارش‌دهی برای تسک‌ها

**جزییات:**
- هر پروژه دارای ستون‌های پیش‌فرض: "برای انجام"، "در دست انجام"، "انجام شده"
- امکان اختصاص کاربران به ستون‌ها
- امکان اختصاص چند کاربر به یک تسک
- سیستم Label برای دسته‌بندی تسک‌ها

#### 5. همگام‌سازی داده‌ها

**نیازمندی‌ها:**
- همگام‌سازی خودکار از API Gateway
- همگام‌سازی دستی
- مدیریت وضعیت Sync
- لاگ‌گیری از عملیات Sync

**منابع داده:**
1. **Faculty Data (دانشکده‌ها)**
   - API Endpoint: `/Faculty`
   - نیاز به Authentication Token
   - Pagination Support
   
2. **Students Data (دانشجویان)**
   - API Endpoint: `/Students`
   - نیاز به Authentication Token
   - فیلتر بر اساس پردیس و ترم
   
3. **LMS Data (سیستم مدیریت یادگیری)**
   - 11 Zone مختلف
   - URL های مختلف برای هر Zone
   - همگام‌سازی مداوم (Continuous Sync)

**نحوه همگام‌سازی:**
- برای Faculty و Students: همگام‌سازی دوره‌ای (Scheduled)
- برای LMS: همگام‌سازی مداوم (Continuous Thread)
- امکان توقف و شروع مجدد
- مدیریت خطا و Retry

### نیازمندی‌های غیرعملکردی

#### 1. عملکرد (Performance)

- زمان بارگذاری داشبوردها: کمتر از 3 ثانیه
- Cache برای کاهش بار روی دیتابیس
- بهینه‌سازی Query ها
- محدودیت تعداد رکوردهای بازگشتی (50000)

#### 2. امنیت (Security)

- استفاده از HTTPS
- Session Security (Secure, HttpOnly, SameSite)
- SQL Injection Prevention (SQLAlchemy ORM)
- CSRF Protection
- Authentication Token Management
- رمزنگاری اطلاعات حساس

#### 3. قابلیت اطمینان (Reliability)

- Error Handling جامع
- Logging کامل
- Backup دیتابیس
- Recovery از خطاها

#### 4. قابلیت نگهداری (Maintainability)

- کد ماژولار و قابل توسعه
- مستندات کامل
- استفاده از Design Patterns
- Separation of Concerns

#### 5. قابلیت استفاده (Usability)

- رابط کاربری فارسی و RTL
- پشتیبانی از تقویم شمسی
- پیام‌های خطای واضح
- راهنمای کاربر

---

## تحلیل ذینفعان

### 1. کاربران نهایی

**سازمان مرکزی:**
- نیاز: مشاهده آمار کلی کشور
- دسترسی: تمام داده‌ها
- داشبوردها: همه داشبوردها با فیلتر کامل

**دانشگاه استان:**
- نیاز: مشاهده آمار استان خود
- دسترسی: فقط داده‌های استان
- داشبوردها: همه داشبوردها با فیلتر استان

**دانشکده:**
- نیاز: مشاهده آمار دانشکده خود
- دسترسی: فقط داده‌های دانشکده
- داشبوردها: همه داشبوردها با فیلتر دانشکده

**مدیر سیستم:**
- نیاز: مدیریت کامل سیستم
- دسترسی: تمام داده‌ها + پنل مدیریت
- قابلیت‌ها: مدیریت کاربران، تنظیمات، همگام‌سازی

### 2. توسعه‌دهندگان

- نیاز: مستندات کامل، کد قابل نگهداری
- مسئولیت: توسعه و نگهداری سیستم

### 3. مدیران پروژه

- نیاز: سیستم Kanban برای مدیریت پروژه‌ها
- قابلیت‌ها: ایجاد پروژه، اختصاص تسک، پیگیری پیشرفت

---

## تحلیل فرآیندهای کسب‌وکار

### فرآیند 1: ورود کاربر

```
1. کاربر به صفحه اصلی می‌رود
2. سیستم بررسی می‌کند که آیا کاربر لاگین است
3. اگر لاگین نیست، به صفحه SSO هدایت می‌شود
4. کاربر در SSO احراز هویت می‌کند
5. SSO کاربر را به سیستم برمی‌گرداند با Token
6. سیستم Token را بررسی می‌کند
7. اطلاعات کاربر از SSO دریافت می‌شود
8. کاربر در دیتابیس ایجاد یا به‌روزرسانی می‌شود
9. Session ایجاد می‌شود
10. کاربر به صفحه اصلی هدایت می‌شود
```

### فرآیند 2: مشاهده داشبورد

```
1. کاربر داشبورد را انتخاب می‌کند
2. سیستم بررسی دسترسی کاربر می‌کند
3. اگر دسترسی ندارد، خطا نمایش داده می‌شود
4. اگر دسترسی دارد:
   a. فیلترهای دسترسی کاربر اعمال می‌شود
   b. داده‌ها از Data Provider دریافت می‌شوند
   c. داده‌ها پردازش می‌شوند
   d. Cache بررسی می‌شود
   e. اگر Cache موجود است، از Cache استفاده می‌شود
   f. اگر Cache موجود نیست، داده‌ها از دیتابیس خوانده می‌شوند
   g. داده‌ها به Template ارسال می‌شوند
   h. داشبورد رندر می‌شود
```

### فرآیند 3: همگام‌سازی داده‌ها

#### همگام‌سازی Faculty/Students:

```
1. Scheduler بررسی می‌کند که آیا زمان Sync رسیده است
2. اگر رسیده است:
   a. وضعیت Sync به "running" تغییر می‌کند
   b. Authentication Token از API دریافت می‌شود
   c. داده‌ها از API دریافت می‌شوند (با Pagination)
   d. داده‌ها در دیتابیس ذخیره می‌شوند
   e. تعداد رکوردهای Sync شده ثبت می‌شود
   f. زمان Sync بعدی محاسبه می‌شود
   g. وضعیت Sync به "success" یا "failed" تغییر می‌کند
   h. خطاها در صورت وجود ثبت می‌شوند
```

#### همگام‌سازی LMS (Continuous):

```
1. Thread همگام‌سازی LMS شروع می‌شود
2. Thread در یک حلقه بی‌نهایت اجرا می‌شود:
   a. برای هر Zone:
      - داده‌ها از URL Zone دریافت می‌شوند
      - داده‌ها Parse می‌شوند
      - داده‌ها در دیتابیس ذخیره می‌شوند
   b. Thread به مدت مشخص (مثلاً 60 ثانیه) Sleep می‌کند
   c. اگر Stop درخواست شده باشد، Thread متوقف می‌شود
3. در صورت خطا، Thread دوباره شروع می‌شود
```

### فرآیند 4: مدیریت پروژه

```
1. کاربر پروژه جدید ایجاد می‌کند
2. سیستم ستون‌های پیش‌فرض Kanban را ایجاد می‌کند
3. کاربر می‌تواند:
   a. تسک‌ها را ایجاد کند
   b. تسک‌ها را بین ستون‌ها جابجا کند
   c. کاربران را به ستون‌ها اختصاص دهد
   d. کاربران را به تسک‌ها اختصاص دهد
   e. برچسب‌ها را به تسک‌ها اختصاص دهد
   f. گزارش برای تسک‌ها بنویسد
```

---

## تحلیل داده‌ها

### ساختار داده‌ها

#### 1. داده‌های کاربران

**جدول users:**
- id: شناسه یکتا
- name: نام کاربر
- email: ایمیل
- sso_id: شناسه SSO (منحصر به فرد)
- province_code: کد استان
- university_code: کد دانشگاه
- faculty_code: کد دانشکده

**جدول access_levels:**
- id: شناسه یکتا
- user_id: شناسه کاربر
- level: سطح دسترسی (admin, central_org, province_university, faculty)

#### 2. داده‌های پروژه‌ها

**جدول projects:**
- id: شناسه یکتا
- title: عنوان پروژه
- description: توضیحات
- creator_id: شناسه ایجادکننده
- owner_id: شناسه مالک
- start_date: تاریخ شروع
- end_date: تاریخ پایان
- updated_at: تاریخ به‌روزرسانی

**جدول kanban_columns:**
- id: شناسه یکتا
- project_id: شناسه پروژه
- title: عنوان ستون
- order: ترتیب نمایش

**جدول tasks:**
- id: شناسه یکتا
- column_id: شناسه ستون
- project_id: شناسه پروژه
- title: عنوان تسک
- description: توضیحات
- due_date: تاریخ سررسید
- start_date: تاریخ شروع

#### 3. داده‌های دانشکده‌ها

**جدول faculty (در faculty_data.db):**
- code_markaz: کد مرکز (کلید اصلی)
- name: نام دانشکده
- province_code: کد استان
- university_code: کد دانشگاه
- ... (سایر فیلدها)

#### 4. داده‌های LMS

**جدول monitor_data (در faculty_data.db):**
- id: شناسه یکتا
- url: URL منبع (Zone)
- timestamp: زمان ثبت
- key: کلید داده (online_lms_user, online_adobe_class, ...)
- value: مقدار

### روابط داده‌ها

```
users (1) ──< (N) access_levels
users (1) ──< (N) projects (creator)
users (1) ──< (N) projects (owner)
users (N) ──< (M) projects (members)
users (N) ──< (M) kanban_columns
users (N) ──< (M) tasks
projects (1) ──< (N) kanban_columns
kanban_columns (1) ──< (N) tasks
tasks (1) ──< (N) task_label_assignments
tasks (1) ──< (N) reports
```

### جریان داده‌ها

```
API Gateway → Authentication → Fetch Data → Parse → Store in DB → Dashboard Query → Process → Cache → Display
```

---

## تحلیل امنیت

### تهدیدات و راه‌حل‌ها

#### 1. تهدید: دسترسی غیرمجاز

**راه‌حل:**
- استفاده از SSO برای احراز هویت
- کنترل دسترسی بر اساس سطح سازمانی
- بررسی دسترسی در هر درخواست
- لاگ‌گیری از دسترسی‌ها

#### 2. تهدید: Session Hijacking

**راه‌حل:**
- استفاده از Secure Cookies
- HttpOnly Flag
- SameSite Protection
- Timeout برای Session

#### 3. تهدید: SQL Injection

**راه‌حل:**
- استفاده از SQLAlchemy ORM
- Parameterized Queries
- Validation ورودی‌ها

#### 4. تهدید: CSRF

**راه‌حل:**
- استفاده از State در OAuth Flow
- بررسی State در Callback
- استفاده از CSRF Tokens (در صورت نیاز)

#### 5. تهدید: اطلاعات حساس

**راه‌حل:**
- ذخیره اطلاعات حساس در Environment Variables
- عدم Hardcode کردن Credentials
- رمزنگاری اطلاعات حساس در دیتابیس (در صورت نیاز)

### سیاست‌های امنیتی

1. **حداقل دسترسی (Least Privilege)**
   - هر کاربر فقط به داده‌های مورد نیاز دسترسی دارد
   - فیلترهای دسترسی در سطح Query اعمال می‌شوند

2. **Audit Trail**
   - تمام عملیات مهم در جدول `access_logs` ثبت می‌شوند
   - شامل: IP Address, User Agent, Action, Resource

3. **Session Management**
   - Session Timeout
   - Secure Cookie Settings
   - Logout کامل

---

## تحلیل عملکرد

### نقاط بحرانی عملکرد

#### 1. Query های پیچیده

**مشکل:**
- Query های داشبوردها ممکن است روی داده‌های زیادی اجرا شوند
- Join های متعدد

**راه‌حل:**
- استفاده از Index ها
- محدود کردن تعداد رکوردهای بازگشتی
- استفاده از Cache

#### 2. همگام‌سازی داده‌ها

**مشکل:**
- همگام‌سازی ممکن است زمان‌بر باشد
- ممکن است سیستم را Block کند

**راه‌حل:**
- اجرای Sync در Background Thread
- استفاده از Pagination
- مدیریت خطا و Retry

#### 3. Cache Management

**راه‌حل:**
- Cache برای داشبوردها با TTL قابل تنظیم
- Invalidation Cache در صورت تغییر داده‌ها
- استفاده از Memory Cache

### معیارهای عملکرد

- زمان بارگذاری داشبورد: < 3 ثانیه
- زمان همگام‌سازی: بستگی به حجم داده دارد
- استفاده از حافظه: بهینه با Cache محدود

---

## تحلیل قابلیت‌های سیستم

### قابلیت‌های اصلی

1. **احراز هویت و مدیریت دسترسی**
   - ✅ SSO Integration
   - ✅ Role-based Access Control
   - ✅ Session Management

2. **سیستم داشبوردها**
   - ✅ معماری ماژولار
   - ✅ Cache System
   - ✅ فیلترینگ داده‌ها
   - ✅ پشتیبانی از نمودارها و نقشه‌ها

3. **پنل مدیریت**
   - ✅ مدیریت کاربران
   - ✅ مدیریت دسترسی
   - ✅ مدیریت همگام‌سازی
   - ✅ مشاهده لاگ‌ها

4. **مدیریت پروژه**
   - ✅ سیستم Kanban
   - ✅ مدیریت تسک‌ها
   - ✅ سیستم برچسب‌گذاری

5. **همگام‌سازی داده‌ها**
   - ✅ همگام‌سازی خودکار
   - ✅ همگام‌سازی دستی
   - ✅ مدیریت وضعیت

### قابلیت‌های آینده

- Export به Excel/PDF
- Real-time Updates (WebSocket)
- Mobile Responsive
- داشبوردهای جدید
- گزارش‌گیری پیشرفته

---

## نتیجه‌گیری

این سیستم یک پلتفرم جامع BI برای دانشگاه است که با معماری ماژولار و قابل توسعه طراحی شده است. سیستم از امنیت و عملکرد خوبی برخوردار است و قابلیت‌های متنوعی برای مدیریت و تحلیل داده‌های دانشگاهی ارائه می‌دهد.

---

**تاریخ ایجاد**: 1404/01/15
**نسخه**: 1.0.0
**نگهدارنده**: تیم توسعه

