# مستندات تست و امنیت سیستم BI دانشگاه

## 📋 فهرست مطالب

### بخش تست
1. [استراتژی تست](#استراتژی-تست)
2. [تست‌های واحد (Unit Tests)](#تست‌های-واحد-unit-tests)
3. [تست‌های یکپارچگی (Integration Tests)](#تست‌های-یکپارچگی-integration-tests)
4. [تست‌های عملکردی (Functional Tests)](#تست‌های-عملکردی-functional-tests)
5. [تست‌های امنیتی (Security Tests)](#تست‌های-امنیتی-security-tests)

### بخش امنیت
6. [معماری امنیتی](#معماری-امنیتی)
7. [احراز هویت و مجوزدهی](#احراز-هویت-و-مجوزدهی)
8. [محافظت از داده‌ها](#محافظت-از-داده‌ها)
9. [امنیت شبکه](#امنیت-شبکه)
10. [Audit و Logging](#audit-و-logging)

---

## استراتژی تست

### انواع تست

```
┌─────────────────────────────────────┐
│         Unit Tests (70%)            │
│  - Models                            │
│  - Data Providers                   │
│  - Utilities                        │
└─────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────┐
│    Integration Tests (20%)          │
│  - API Endpoints                    │
│  - Database Operations              │
│  - Sync Handlers                    │
└─────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────┐
│      Functional Tests (10%)         │
│  - User Flows                       │
│  - Dashboard Rendering              │
│  - Admin Panel                      │
└─────────────────────────────────────┘
```

### Coverage Goals

- **Unit Tests**: > 80%
- **Integration Tests**: > 60%
- **Critical Paths**: 100%

---

## تست‌های واحد (Unit Tests)

### ساختار تست‌ها

```
tests/
├── __init__.py
├── test_models.py
├── test_data_providers.py
├── test_dashboards.py
├── test_auth.py
├── test_cache.py
└── test_utils.py
```

### نمونه: تست Models

```python
# tests/test_models.py
import unittest
from app import app
from extensions import db
from models import User, AccessLevel

class TestUserModel(unittest.TestCase):
    def setUp(self):
        self.app = app
        self.app.config['TESTING'] = True
        self.app.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite:///:memory:'
        self.client = self.app.test_client()
        with self.app.app_context():
            db.create_all()
    
    def tearDown(self):
        with self.app.app_context():
            db.drop_all()
    
    def test_create_user(self):
        with self.app.app_context():
            user = User(
                sso_id='test_user',
                name='Test User',
                email='test@example.com'
            )
            db.session.add(user)
            db.session.commit()
            
            self.assertIsNotNone(user.id)
            self.assertEqual(user.sso_id, 'test_user')
    
    def test_user_is_admin(self):
        with self.app.app_context():
            user = User(sso_id='admin', name='Admin')
            db.session.add(user)
            db.session.flush()
            
            access = AccessLevel(level='admin', user_id=user.id)
            db.session.add(access)
            db.session.commit()
            
            self.assertTrue(user.is_admin())
```

### نمونه: تست Data Providers

```python
# tests/test_data_providers.py
import unittest
from app import app
from dashboards.data_providers import FacultyDataProvider
from dashboards.context import UserContext
from models import User

class TestFacultyDataProvider(unittest.TestCase):
    def setUp(self):
        self.app = app
        self.app.config['TESTING'] = True
        self.provider = FacultyDataProvider()
    
    def test_execute_query(self):
        query = "SELECT COUNT(*) as count FROM faculty"
        results = self.provider.execute_query(query)
        self.assertIsInstance(results, list)
    
    def test_apply_context_filters(self):
        user = User(sso_id='test', name='Test')
        context = UserContext(user)
        context.data_filters['province_code'] = 1
        
        query = "SELECT * FROM faculty"
        params = ()
        modified_query, modified_params = self.provider._apply_context_filters(
            query, params, context
        )
        
        self.assertIn('WHERE', modified_query)
        self.assertIn('province_code', modified_query)
```

### نمونه: تست Cache

```python
# tests/test_cache.py
import unittest
from dashboards.cache import DashboardCache
from datetime import datetime, timedelta

class TestDashboardCache(unittest.TestCase):
    def setUp(self):
        DashboardCache.clear()
    
    def test_set_and_get(self):
        DashboardCache.set('test_key', {'data': 'test'}, ttl=60)
        result = DashboardCache.get('test_key')
        self.assertEqual(result, {'data': 'test'})
    
    def test_cache_expiration(self):
        DashboardCache.set('test_key', {'data': 'test'}, ttl=1)
        import time
        time.sleep(2)
        result = DashboardCache.get('test_key')
        self.assertIsNone(result)
    
    def test_generate_key(self):
        key = DashboardCache.generate_key('test', a=1, b=2)
        self.assertIsInstance(key, str)
        self.assertTrue(key.startswith('test:'))
```

---

## تست‌های یکپارچگی (Integration Tests)

### نمونه: تست API Endpoints

```python
# tests/test_integration.py
import unittest
from app import app
from extensions import db
from models import User
from flask_login import login_user

class TestDashboardAPI(unittest.TestCase):
    def setUp(self):
        self.app = app
        self.app.config['TESTING'] = True
        self.app.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite:///:memory:'
        self.client = self.app.test_client()
        with self.app.app_context():
            db.create_all()
            # Create test user
            user = User(sso_id='test', name='Test')
            db.session.add(user)
            db.session.commit()
            self.user = user
    
    def test_dashboard_list_requires_auth(self):
        response = self.client.get('/dashboards/')
        self.assertEqual(response.status_code, 302)  # Redirect to login
    
    def test_dashboard_list_authenticated(self):
        with self.client:
            with self.app.app_context():
                login_user(self.user)
                response = self.client.get('/dashboards/')
                self.assertEqual(response.status_code, 200)
    
    def test_dashboard_access_control(self):
        with self.client:
            with self.app.app_context():
                login_user(self.user)
                # Test accessing dashboard without permission
                response = self.client.get('/dashboards/restricted_dashboard')
                # Should return 403 or redirect
                self.assertIn(response.status_code, [403, 302])
```

### نمونه: تست Data Sync

```python
# tests/test_sync.py
import unittest
from app import app
from extensions import db
from admin_models import DataSync
from admin.sync_handlers import run_faculty_sync

class TestDataSync(unittest.TestCase):
    def setUp(self):
        self.app = app
        self.app.config['TESTING'] = True
        with self.app.app_context():
            db.create_all()
            # Create test sync config
            sync = DataSync(
                data_source='faculty',
                auto_sync_enabled=False,
                api_endpoint='http://test-api/faculty'
            )
            db.session.add(sync)
            db.session.commit()
    
    def test_sync_configuration(self):
        with self.app.app_context():
            sync = DataSync.query.filter_by(data_source='faculty').first()
            self.assertIsNotNone(sync)
            self.assertEqual(sync.data_source, 'faculty')
```

---

## تست‌های عملکردی (Functional Tests)

### نمونه: تست User Flow

```python
# tests/test_user_flows.py
import unittest
from app import app
from extensions import db
from models import User

class TestUserFlow(unittest.TestCase):
    def setUp(self):
        self.app = app
        self.app.config['TESTING'] = True
        self.client = self.app.test_client()
    
    def test_complete_user_flow(self):
        # 1. User visits homepage
        response = self.client.get('/')
        self.assertEqual(response.status_code, 302)  # Redirect to login
        
        # 2. User logs in (mock SSO)
        # ... (mock SSO flow)
        
        # 3. User views dashboard list
        # ... (check dashboard list)
        
        # 4. User views specific dashboard
        # ... (check dashboard rendering)
```

---

## تست‌های امنیتی (Security Tests)

### نمونه: تست Authentication

```python
# tests/test_security.py
import unittest
from app import app

class TestSecurity(unittest.TestCase):
    def setUp(self):
        self.app = app
        self.app.config['TESTING'] = True
        self.client = self.app.test_client()
    
    def test_csrf_protection(self):
        # Test OAuth state validation
        response = self.client.get('/authorized?state=invalid_state')
        self.assertEqual(response.status_code, 400)
    
    def test_session_security(self):
        # Test secure cookie settings
        response = self.client.get('/')
        cookies = response.headers.getlist('Set-Cookie')
        # Check for Secure, HttpOnly flags
        self.assertTrue(any('Secure' in c for c in cookies))
    
    def test_sql_injection_prevention(self):
        # Test that user input is properly escaped
        malicious_input = "'; DROP TABLE users; --"
        # Should not cause SQL injection
        # (test with actual query)
```

---

## معماری امنیتی

### Security Layers

```
┌─────────────────────────────────────┐
│    Network Security (HTTPS)          │
│    - TLS 1.2+                        │
│    - Certificate Validation          │
└─────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────┐
│    Application Security              │
│    - Input Validation                │
│    - Output Encoding                 │
│    - Error Handling                  │
└─────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────┐
│    Authentication                    │
│    - SSO Integration                 │
│    - Session Management              │
│    - Token Validation                │
└─────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────┐
│    Authorization                     │
│    - Role-based Access Control       │
│    - Context-based Filtering        │
│    - Dashboard Access Control       │
└─────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────┐
│    Data Protection                   │
│    - Encryption at Rest             │
│    - Encryption in Transit           │
│    - Access Logging                  │
└─────────────────────────────────────┘
```

---

## احراز هویت و مجوزدهی

### SSO Integration

```python
# app/app.py
# OAuth2 Configuration
SSO_CONFIG = {
    "client_id": os.getenv("SSO_CLIENT_ID"),
    "client_secret": os.getenv("SSO_CLIENT_SECRET"),  # از Environment Variable
    "authorize_url": os.getenv("SSO_AUTH_URL"),
    "access_token_url": os.getenv("SSO_TOKEN_URL"),
    "userinfo_url": "https://sso.cfu.ac.ir/oauth2/userinfo",
    "redirect_uri": os.getenv("SSO_REDIRECT_URI"),
}

# CSRF Protection
@app.route("/authorized")
def authorized():
    stored_state = session.get("oauth_state")
    returned_state = request.args.get("state")
    
    if stored_state != returned_state:
        return render_template("error.html", error="CSRF Warning!"), 400
    # ...
```

### Session Security

```python
# app/app.py
app.config["SESSION_COOKIE_SECURE"] = True      # فقط HTTPS
app.config["SESSION_COOKIE_HTTPONLY"] = True    # جلوگیری از JavaScript
app.config["SESSION_COOKIE_SAMESITE"] = "Lax"   # CSRF Protection
```

### Role-based Access Control

```python
# app/dashboards/context.py
class UserContext:
    def _determine_access_level(self) -> AccessLevel:
        if self.user.is_admin():
            return AccessLevel.ADMIN
        
        access_levels = [acc.level.lower() for acc in self.user.access_levels]
        
        if 'central_org' in access_levels:
            return AccessLevel.CENTRAL_ORG
        # ...
    
    def apply_filters(self, query_filters: Dict[str, Any]) -> Dict[str, Any]:
        # اعمال فیلترهای دسترسی در سطح Query
        if self.data_filters['province_code']:
            query_filters['province_code'] = self.data_filters['province_code']
        # ...
```

---

## محافظت از داده‌ها

### SQL Injection Prevention

```python
# استفاده از SQLAlchemy ORM (Parameterized Queries)
user = User.query.filter_by(sso_id=user_input).first()

# یا در صورت نیاز به Raw SQL
query = "SELECT * FROM users WHERE sso_id = ?"
cursor.execute(query, (user_input,))  # Parameterized
```

### XSS Prevention

```python
# Jinja2 به صورت خودکار Escape می‌کند
{{ user_input }}  # Safe

# در صورت نیاز به Raw HTML
{{ user_input | safe }}  # فقط در صورت اطمینان
```

### Input Validation

```python
# app/admin/routes.py
@app.route('/users/new', methods=['POST'])
def user_create():
    sso_id = request.form.get('sso_id')
    
    # Validation
    if not sso_id or len(sso_id) < 3:
        flash('SSO ID باید حداقل 3 کاراکتر باشد', 'error')
        return render_template('admin/users/create.html')
    
    # Check for existing user
    if User.query.filter_by(sso_id=sso_id).first():
        flash('کاربری با این SSO ID وجود دارد', 'error')
        return render_template('admin/users/create.html')
    
    # Create user
    # ...
```

---

## امنیت شبکه

### HTTPS Configuration

```nginx
# Nginx Configuration
server {
    listen 443 ssl http2;
    server_name bi.cfu.ac.ir;
    
    ssl_certificate /etc/letsencrypt/live/bi.cfu.ac.ir/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/bi.cfu.ac.ir/privkey.pem;
    
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    
    # HSTS
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
}
```

### Firewall Rules

```bash
# فقط پورت‌های لازم را باز کنید
sudo ufw allow 22/tcp    # SSH
sudo ufw allow 80/tcp    # HTTP (redirect to HTTPS)
sudo ufw allow 443/tcp   # HTTPS
sudo ufw enable
```

---

## Audit و Logging

### Access Logging

```python
# app/admin/utils.py
def log_action(action, resource_type, resource_id, details=None, user_id=None):
    """Log user actions for audit trail"""
    from admin_models import AccessLog
    from flask import request
    from flask_login import current_user
    
    log = AccessLog(
        user_id=user_id or current_user.id,
        action=action,
        resource_type=resource_type,
        resource_id=str(resource_id),
        ip_address=request.remote_addr,
        user_agent=request.headers.get('User-Agent'),
        request_path=request.path,
        request_method=request.method,
        details=details or {}
    )
    db.session.add(log)
    db.session.commit()
```

### Security Event Logging

```python
# Log security events
logger.warning(f"Failed login attempt: {username} from {ip_address}")
logger.error(f"Unauthorized access attempt: {user_id} tried to access {resource}")
logger.info(f"User {user_id} granted admin access")
```

---

## نتیجه‌گیری

این مستند راهنمای کامل تست و امنیت سیستم است. با دنبال کردن این راهنما، می‌توانید اطمینان حاصل کنید که سیستم به درستی تست شده و امن است.

---

**تاریخ ایجاد**: 1404/01/15
**نسخه**: 1.0.0
**نگهدارنده**: تیم توسعه




