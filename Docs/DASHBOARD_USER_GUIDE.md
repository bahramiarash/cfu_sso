# راهنمای استفاده از سیستم داشبوردها برای کاربران

## 🎯 سطوح دسترسی

سیستم داشبوردها بر اساس سطح دسترسی کاربر، داده‌های مناسب را نمایش می‌دهد:

### 1. سازمان مرکزی (Central Organization)
- **دسترسی**: تمام داده‌های کشور
- **قابلیت فیلتر**: می‌تواند بر اساس استان، دانشگاه، یا دانشکده فیلتر کند
- **نمایش**: داده‌های کل کشور به صورت پیش‌فرض

### 2. دانشگاه استان (Province University)
- **دسترسی**: فقط داده‌های استان خود
- **قابلیت فیلتر**: می‌تواند بر اساس دانشکده فیلتر کند
- **نمایش**: داده‌های استان به صورت پیش‌فرض

### 3. دانشکده (Faculty)
- **دسترسی**: فقط داده‌های دانشکده خود
- **قابلیت فیلتر**: ندارد
- **نمایش**: داده‌های دانشکده به صورت پیش‌فرض

### 4. مدیر سیستم (Admin)
- **دسترسی**: تمام داده‌ها
- **قابلیت فیلتر**: تمام فیلترها
- **نمایش**: تمام داده‌ها

---

## 📊 استفاده از داشبوردها

### مشاهده لیست داشبوردها
1. وارد سیستم شوید
2. به بخش "داشبوردها" بروید
3. لیست داشبوردهای قابل دسترسی شما نمایش داده می‌شود

### مشاهده داشبورد
1. روی نام داشبورد مورد نظر کلیک کنید
2. داده‌ها بر اساس سطح دسترسی شما نمایش داده می‌شوند

### فیلتر کردن داده‌ها (فقط برای کاربران مرکزی)
1. در داشبورد، از dropdown "استان" استفاده کنید
2. استان مورد نظر را انتخاب کنید
3. داده‌ها به‌روزرسانی می‌شوند

---

## 🔧 تنظیم سطح دسترسی (برای مدیران)

### تنظیم سطح دسترسی کاربر

```python
from models import User, AccessLevel, db

# پیدا کردن کاربر
user = User.query.filter_by(sso_id="username").first()

# تنظیم سطح دسترسی
access = AccessLevel(level="province_university", user_id=user.id)
db.session.add(access)

# تنظیم اطلاعات سازمانی
user.province_code = 1  # کد استان (مثلاً 1 برای تهران)
user.university_code = 101  # کد دانشگاه
user.faculty_code = 1001  # کد دانشکده (code_markaz)

db.session.commit()
```

### سطوح دسترسی موجود:
- `"central_org"` - سازمان مرکزی
- `"province_university"` - دانشگاه استان
- `"faculty"` - دانشکده
- `"admin"` - مدیر سیستم

---

## 📝 مثال‌ها

### مثال 1: کاربر سازمان مرکزی
```python
user = User.query.filter_by(sso_id="admin_user").first()
access = AccessLevel(level="central_org", user_id=user.id)
db.session.add(access)
db.session.commit()
```

### مثال 2: کاربر دانشگاه استان
```python
user = User.query.filter_by(sso_id="province_user").first()
access = AccessLevel(level="province_university", user_id=user.id)
user.province_code = 1  # تهران
db.session.add(access)
db.session.commit()
```

### مثال 3: کاربر دانشکده
```python
user = User.query.filter_by(sso_id="faculty_user").first()
access = AccessLevel(level="faculty", user_id=user.id)
user.province_code = 1  # تهران
user.faculty_code = 1001  # دانشکده خاص
db.session.add(access)
db.session.commit()
```

---

## ⚠️ نکات مهم

1. **فیلتر خودکار**: داده‌ها به صورت خودکار بر اساس سطح دسترسی فیلتر می‌شوند
2. **Cache**: داده‌ها برای 5-10 دقیقه cache می‌شوند
3. **امنیت**: کاربران نمی‌توانند داده‌های خارج از سطح دسترسی خود را ببینند
4. **به‌روزرسانی**: برای به‌روزرسانی فوری، cache را پاک کنید

---

## 🐛 عیب‌یابی

### مشکل: کاربر نمی‌تواند داشبورد را ببیند
- بررسی کنید که سطح دسترسی کاربر تنظیم شده باشد
- بررسی کنید که کاربر در دیتابیس وجود دارد

### مشکل: داده‌های اشتباه نمایش داده می‌شود
- بررسی کنید که province_code, university_code, faculty_code به درستی تنظیم شده باشند
- Cache را پاک کنید

### مشکل: فیلتر کار نمی‌کند
- بررسی کنید که سطح دسترسی کاربر "central_org" یا "admin" باشد
- بررسی کنید که فیلترها به درستی ارسال می‌شوند


