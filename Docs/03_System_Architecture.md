# معماری سیستم

## 📋 فهرست مطالب

1. [معماری کلی](#معماری-کلی)
2. [معماری لایه‌ای](#معماری-لایه‌ای)
3. [معماری داشبوردها](#معماری-داشبوردها)
4. [معماری احراز هویت](#معماری-احراز-هویت)
5. [معماری داده](#معماری-داده)
6. [الگوهای طراحی](#الگوهای-طراحی)

---

## معماری کلی

### نمودار معماری سطح بالا

```
┌─────────────────────────────────────────────────────────────┐
│                    Client (Browser)                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   User UI    │  │  Admin UI    │  │  Kanban UI   │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ HTTP/HTTPS
                            │
┌─────────────────────────────────────────────────────────────┐
│                    Flask Application                        │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              Presentation Layer                       │  │
│  │  - Jinja2 Templates                                   │  │
│  │  - Static Files (CSS, JS)                             │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              Application Layer                        │  │
│  │  - Routes (Dashboard, Admin, Kanban)                  │  │
│  │  - Blueprints                                         │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              Business Logic Layer                     │  │
│  │  - Dashboard Classes                                  │  │
│  │  - Data Providers                                     │  │
│  │  - Access Control                                     │  │
│  │  - Sync Handlers                                      │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              Data Access Layer                        │  │
│  │  - SQLAlchemy ORM                                      │  │
│  │  - Database Connections                               │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
┌───────▼──────┐  ┌─────────▼─────────┐  ┌────▼──────────┐
│ SQLite DB 1 │  │  SQLite DB 2       │  │  External API  │
│(access_ctrl)│  │(faculty_data)      │  │  (API Gateway) │
└─────────────┘  └────────────────────┘  └────────────────┘
```

---

## معماری لایه‌ای

### 1. لایه Presentation (ارائه)

**مسئولیت‌ها**:
- نمایش داده‌ها به کاربر
- دریافت ورودی از کاربر
- مدیریت UI/UX

**کامپوننت‌ها**:
- **Templates**: Jinja2 templates در `app/templates/`
- **Static Files**: CSS, JavaScript, Images در `app/static/`
- **Frontend Libraries**: Bootstrap, Chart.js, Plotly

**فایل‌های کلیدی**:
```
app/templates/
├── base.html              # Template پایه
├── dashboard_base.html    # Template پایه داشبوردها
├── admin/
│   └── base.html          # Template پایه پنل ادمین
└── dashboards/
    └── *.html             # Template های داشبوردها
```

### 2. لایه Application (کاربرد)

**مسئولیت‌ها**:
- مدیریت Route ها
- مدیریت Request/Response
- مدیریت Session

**کامپوننت‌ها**:
- **Routes**: Route handlers در `app/app.py` و Blueprints
- **Blueprints**: 
  - `dashboard_bp`: داشبوردها
  - `admin_bp`: پنل ادمین
  - `kanban_bp`: سیستم Kanban

**فایل‌های کلیدی**:
```
app/
├── app.py                 # فایل اصلی و Route های اصلی
├── dashboard_routes.py    # Route های داشبورد
├── admin/
│   └── routes.py         # Route های پنل ادمین
└── kanban.py             # Route های Kanban
```

### 3. لایه Business Logic (منطق کسب‌وکار)

**مسئولیت‌ها**:
- پیاده‌سازی قوانین کسب‌وکار
- پردازش داده‌ها
- مدیریت دسترسی‌ها

**کامپوننت‌ها**:

#### 3.1 سیستم داشبوردها
```
app/dashboards/
├── base.py                # BaseDashboard (Abstract Class)
├── registry.py            # DashboardRegistry (Singleton)
├── context.py             # UserContext (Access Control)
├── cache.py               # DashboardCache
├── dashboards/            # داشبوردهای خاص
│   ├── faculty_stats.py
│   ├── faculty_map.py
│   └── ...
├── data_providers/        # ارائه‌دهندگان داده
│   ├── base.py
│   ├── faculty.py
│   └── students.py
└── visualizations/        # کامپوننت‌های بصری
    └── maps.py
```

#### 3.2 سیستم همگام‌سازی
```
app/admin/
├── sync_handlers.py       # Handler های همگام‌سازی
├── scheduler.py           # Scheduler برای همگام‌سازی خودکار
└── sync_progress.py      # مدیریت پیشرفت همگام‌سازی
```

#### 3.3 سیستم دسترسی
```
app/
├── auth_utils.py          # توابع احراز هویت
├── dashboards/context.py # UserContext
└── admin/utils.py        # توابع دسترسی ادمین
```

### 4. لایه Data Access (دسترسی به داده)

**مسئولیت‌ها**:
- ارتباط با دیتابیس
- ارتباط با API های خارجی
- مدیریت Connection ها

**کامپوننت‌ها**:

#### 4.1 ORM Models
```
app/
├── models.py              # مدل‌های اصلی
│   ├── User
│   ├── Project
│   ├── Task
│   └── ...
└── admin_models.py        # مدل‌های پنل ادمین
    ├── DashboardAccess
    ├── AccessLog
    ├── DataSync
    └── DashboardConfig
```

#### 4.2 Data Providers
```
app/dashboards/data_providers/
├── base.py                # BaseDataProvider
├── faculty.py             # FacultyDataProvider
└── students.py            # StudentsDataProvider
```

#### 4.3 External Data Fetching
```
app/fetch_data/
├── faculty_main.py        # دریافت داده‌های دانشکده
├── students_main.py      # دریافت داده‌های دانشجویان
└── lms_sync.py           # همگام‌سازی LMS
```

---

## معماری داشبوردها

### نمودار جریان داشبورد

```
User Request
    │
    ▼
┌─────────────────┐
│  Route Handler  │
│ (dashboard_routes)│
└────────┬─────────┘
         │
         ▼
┌─────────────────┐
│  Dashboard      │
│  Registry       │
│  (get dashboard)│
└────────┬─────────┘
         │
         ▼
┌─────────────────┐
│  User Context   │
│  (get context)   │
└────────┬─────────┘
         │
         ▼
┌─────────────────┐
│  Check Access   │
│  (permissions)  │
└────────┬─────────┘
         │
         ▼
┌─────────────────┐
│  Check Cache    │
│  (if cached)    │
└────────┬─────────┘
         │
    ┌────┴────┐
    │         │
   Yes       No
    │         │
    ▼         ▼
┌─────────┐ ┌──────────────┐
│ Return  │ │  Get Data    │
│ Cached  │ │  (Provider)   │
│ Data    │ └──────┬───────┘
└─────────┘        │
                   ▼
            ┌──────────────┐
            │  Process     │
            │  Data        │
            └──────┬───────┘
                   │
                   ▼
            ┌──────────────┐
            │  Cache Data  │
            └──────┬───────┘
                   │
                   ▼
            ┌──────────────┐
            │  Render      │
            │  Template    │
            └──────────────┘
```

### کلاس BaseDashboard

```python
class BaseDashboard(ABC):
    """
    کلاس پایه برای تمام داشبوردها
    """
    def __init__(self, dashboard_id, title, description):
        self.dashboard_id = dashboard_id
        self.title = title
        self.description = description
    
    @abstractmethod
    def get_data(self, context, **kwargs):
        """دریافت داده‌ها"""
        pass
    
    @abstractmethod
    def render(self, data, context):
        """رندر کردن template"""
        pass
    
    def handle_request(self, user_context, **kwargs):
        """مدیریت درخواست"""
        # Check access
        # Check cache
        # Get data
        # Render
        pass
```

### Dashboard Registry Pattern

```python
class DashboardRegistry:
    """
    Registry برای مدیریت داشبوردها
    Pattern: Singleton
    """
    _dashboards = {}
    
    @classmethod
    def register(cls, dashboard_class):
        """ثبت داشبورد"""
        instance = dashboard_class()
        cls._dashboards[instance.dashboard_id] = instance
    
    @classmethod
    def get(cls, dashboard_id):
        """دریافت داشبورد"""
        return cls._dashboards.get(dashboard_id)
```

---

## معماری احراز هویت

### نمودار جریان احراز هویت

```
User Access
    │
    ▼
┌─────────────────┐
│  Check Session   │
│  (sso_token)      │
└────────┬─────────┘
         │
    ┌────┴────┐
    │         │
  Exists    Not Exists
    │         │
    ▼         ▼
┌─────────┐ ┌─────────────────┐
│  Load   │ │  Redirect to    │
│  User   │ │  SSO Login      │
└────┬────┘ └────────┬─────────┘
     │               │
     │               ▼
     │        ┌──────────────┐
     │        │  SSO Server  │
     │        │  (OAuth2)    │
     │        └──────┬───────┘
     │               │
     │               ▼
     │        ┌──────────────┐
     │        │  Get Token   │
     │        └──────┬───────┘
     │               │
     └───────────────┘
              │
              ▼
     ┌──────────────┐
     │  Get User    │
     │  Info        │
     └──────┬───────┘
            │
            ▼
     ┌──────────────┐
     │  Create/     │
     │  Update User  │
     └──────┬───────┘
            │
            ▼
     ┌──────────────┐
     │  Set Session │
     └──────────────┘
```

### کامپوننت‌های احراز هویت

1. **OAuth Client**: استفاده از Authlib
2. **Session Management**: Flask-Session
3. **User Management**: Flask-Login
4. **Access Control**: UserContext

---

## معماری داده

### ساختار دیتابیس

#### دیتابیس 1: access_control.db

```
┌─────────────┐
│   users     │
├─────────────┤
│ id          │
│ sso_id      │
│ name        │
│ email       │
│ province_code│
│ university_code│
│ faculty_code│
└─────────────┘
       │
       ├───┐
       │   │
┌──────▼───▼──────┐
│  access_levels  │
├─────────────────┤
│ id              │
│ user_id         │
│ level           │
└─────────────────┘

┌─────────────┐
│  projects   │
├─────────────┤
│ id          │
│ title       │
│ description │
│ creator_id  │
│ owner_id    │
└─────────────┘
       │
       ├───┐
       │   │
┌──────▼───▼──────┐
│ kanban_columns  │
├─────────────────┤
│ id              │
│ project_id      │
│ title           │
│ order           │
└─────────────────┘
       │
       ├───┐
       │   │
┌──────▼───▼──────┐
│     tasks       │
├─────────────────┤
│ id              │
│ column_id       │
│ title           │
│ description     │
│ assignee_id     │
└─────────────────┘
```

#### دیتابیس 2: faculty_data.db

```
┌─────────────┐
│  faculty    │
├─────────────┤
│ id          │
│ name        │
│ province_code│
│ university_code│
│ faculty_code│
│ ...         │
└─────────────┘

┌─────────────┐
│  students   │
├─────────────┤
│ id          │
│ student_id  │
│ name        │
│ faculty_code│
│ ...         │
└─────────────┘

┌─────────────┐
│ monitor_data│
├─────────────┤
│ id          │
│ url         │
│ timestamp   │
│ key         │
│ value       │
└─────────────┘
```

### Data Flow

```
External API (API Gateway)
    │
    ▼
┌─────────────────┐
│  Sync Handler    │
│  (fetch_data)   │
└────────┬─────────┘
         │
         ▼
┌─────────────────┐
│  Process Data   │
│  (transform)    │
└────────┬─────────┘
         │
         ▼
┌─────────────────┐
│  Save to DB     │
│  (faculty_data)│
└────────┬─────────┘
         │
         ▼
┌─────────────────┐
│  Data Provider  │
│  (read from DB) │
└────────┬─────────┘
         │
         ▼
┌─────────────────┐
│  Apply Filters  │
│  (UserContext)  │
└────────┬─────────┘
         │
         ▼
┌─────────────────┐
│  Dashboard      │
│  (display)      │
└─────────────────┘
```

---

## الگوهای طراحی

### 1. Registry Pattern
- **استفاده**: DashboardRegistry
- **هدف**: مدیریت متمرکز داشبوردها

### 2. Factory Pattern
- **استفاده**: ایجاد Data Providers
- **هدف**: ایجاد اشیاء بر اساس نوع داده

### 3. Strategy Pattern
- **استفاده**: انواع مختلف Data Providers
- **هدف**: قابلیت تعویض الگوریتم‌ها

### 4. Template Method Pattern
- **استفاده**: BaseDashboard
- **هدف**: تعریف ساختار کلی و اجازه Override

### 5. Singleton Pattern
- **استفاده**: DashboardRegistry, Cache
- **هدف**: اطمینان از یک نمونه واحد

### 6. Decorator Pattern
- **استفاده**: @requires_auth, @admin_required
- **هدف**: افزودن قابلیت به توابع

---

## امنیت

### 1. احراز هویت
- OAuth2 با SSO
- Session Management
- Token Validation

### 2. کنترل دسترسی
- Role-Based Access Control (RBAC)
- Context-Based Filtering
- Permission Checking

### 3. امنیت داده
- SQL Injection Prevention (SQLAlchemy)
- XSS Prevention (Jinja2 Auto-escape)
- CSRF Protection (Flask-WTF)

### 4. لاگ‌گیری
- Access Logging
- Error Logging
- Audit Trail

---

**تاریخ ایجاد**: 1404/01/XX  
**آخرین به‌روزرسانی**: 1404/01/XX
