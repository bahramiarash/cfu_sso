# مستندات طراحی کامل سیستم BI دانشگاه

## 📋 فهرست مطالب

1. [معماری کلی سیستم](#معماری-کلی-سیستم)
2. [طراحی معماری لایه‌ای](#طراحی-معماری-لایه‌ای)
3. [طراحی ماژول‌ها](#طراحی-ماژول‌ها)
4. [طراحی پایگاه داده](#طراحی-پایگاه-داده)
5. [طراحی API](#طراحی-api)
6. [طراحی امنیت](#طراحی-امنیت)
7. [طراحی UI/UX](#طراحی-uiux)
8. [Design Patterns استفاده شده](#design-patterns-استفاده-شده)
9. [نمودارهای UML](#نمودارهای-uml)

---

## معماری کلی سیستم

### معماری سه‌لایه

```
┌─────────────────────────────────────────────────────────┐
│                  Presentation Layer                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │   Templates  │  │ Static Files│  │   JavaScript │  │
│  │   (Jinja2)   │  │   (CSS/JS)  │  │   (jQuery)   │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│                 Application Layer                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │   Routes     │  │  Blueprints   │  │   Business   │ │
│  │   (Flask)    │  │   (Modules)  │  │    Logic     │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │  Dashboards  │  │ Admin Panel  │  │  Data Sync   │ │
│  │   System     │  │              │  │   System     │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│                    Data Layer                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │   Models     │  │ Data Providers│ │  Database    │ │
│  │ (SQLAlchemy) │  │              │  │  (SQLite)    │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
└─────────────────────────────────────────────────────────┘
```

### کامپوننت‌های اصلی

```
┌─────────────────────────────────────────────────────────┐
│                    Flask Application                     │
│                                                          │
│  ┌──────────────────────────────────────────────────┐ │
│  │         Authentication & Authorization             │ │
│  │  - SSO Integration (OAuth2)                      │ │
│  │  - Flask-Login                                    │ │
│  │  - User Context Management                       │ │
│  └──────────────────────────────────────────────────┘ │
│                                                          │
│  ┌──────────────────────────────────────────────────┐ │
│  │              Dashboard System                     │ │
│  │  - Base Dashboard (Abstract)                      │ │
│  │  - Dashboard Registry                            │ │
│  │  - Data Providers                                │ │
│  │  - Visualization Components                      │ │
│  │  - Cache System                                 │ │
│  └──────────────────────────────────────────────────┘ │
│                                                          │
│  ┌──────────────────────────────────────────────────┐ │
│  │                Admin Panel                        │ │
│  │  - User Management                               │ │
│  │  - Dashboard Access Control                     │ │
│  │  - Data Sync Management                          │ │
│  │  - Access Logging                                │ │
│  └──────────────────────────────────────────────────┘ │
│                                                          │
│  ┌──────────────────────────────────────────────────┐ │
│  │            Project Management                     │ │
│  │  - Kanban Board                                  │ │
│  │  - Task Management                               │ │
│  │  - Label System                                  │ │
│  └──────────────────────────────────────────────────┘ │
│                                                          │
│  ┌──────────────────────────────────────────────────┐ │
│  │          Data Synchronization                     │ │
│  │  - API Gateway Integration                       │ │
│  │  - Scheduled Sync (Scheduler)                     │ │
│  │  - Continuous Sync (Thread)                      │ │
│  │  - Sync Handlers                                 │ │
│  └──────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

---

## طراحی معماری لایه‌ای

### لایه Presentation

**مسئولیت‌ها:**
- نمایش داده‌ها به کاربر
- دریافت ورودی از کاربر
- مدیریت تعاملات کاربر

**کامپوننت‌ها:**
- **Templates (Jinja2)**
  - `base.html`: Template پایه
  - `dashboard_base.html`: Template پایه داشبوردها
  - `admin/base.html`: Template پایه پنل مدیریت
  - Templates اختصاصی برای هر داشبورد

- **Static Files**
  - CSS: Bootstrap RTL, Custom Styles
  - JavaScript: jQuery, Chart.js, Plotly
  - Fonts: Vazir (فارسی)

- **JavaScript Modules**
  - Dashboard Filters
  - Chart Rendering
  - AJAX Requests

### لایه Application

**مسئولیت‌ها:**
- مدیریت درخواست‌ها
- اجرای منطق کسب‌وکار
- هماهنگی بین کامپوننت‌ها

**کامپوننت‌ها:**

#### 1. Routes & Blueprints

```
app/
├── app.py                    # Main application
├── dashboard_routes.py       # Dashboard routes
├── admin/
│   └── routes.py            # Admin routes
├── kanban.py                # Kanban routes
└── students_dashboard.py    # Students dashboard routes
```

#### 2. Business Logic

- **Authentication Logic**
  - SSO Flow Management
  - Session Management
  - Access Control

- **Dashboard Logic**
  - Data Fetching
  - Data Processing
  - Cache Management
  - Access Filtering

- **Sync Logic**
  - API Communication
  - Data Parsing
  - Database Operations
  - Error Handling

### لایه Data

**مسئولیت‌ها:**
- دسترسی به داده‌ها
- مدیریت دیتابیس
- Cache Management

**کامپوننت‌ها:**

#### 1. Models (SQLAlchemy ORM)

```python
# app/models.py
- User
- Project
- KanbanColumn
- Task
- AccessLevel
- Label
- LabelValue
- TaskLabelAssignment
- Report

# app/admin_models.py
- DashboardAccess
- AccessLog
- DataSync
- DashboardConfig
```

#### 2. Data Providers

```python
# app/dashboards/data_providers/
- base.py              # Base DataProvider (Abstract)
- faculty.py           # FacultyDataProvider
- students.py          # StudentsDataProvider
- pardis.py            # PardisDataProvider
- lms.py               # LMSDataProvider
```

#### 3. Database

- **access_control.db**: اطلاعات کاربران، پروژه‌ها، دسترسی‌ها
- **faculty_data.db**: داده‌های دانشکده‌ها، دانشجویان، LMS

---

## طراحی ماژول‌ها

### ماژول 1: Authentication & Authorization

#### کلاس‌ها و توابع

```python
# app/auth_utils.py
def requires_auth(f):
    """Decorator for authentication check"""

# app/app.py
@app.route("/login")
def login():
    """Initiate SSO authentication"""

@app.route("/authorized")
def authorized():
    """Handle SSO callback"""

@app.route("/logout")
def logout():
    """Log out user"""

# app/dashboards/context.py
class UserContext:
    """User context and access management"""
    - access_level: AccessLevel
    - province_code: int
    - university_code: int
    - faculty_code: int
    - data_filters: Dict
```

#### Flow Diagram

```
User → /login → SSO → /authorized → User Info → Session → Dashboard
```

### ماژول 2: Dashboard System

#### معماری Dashboard

```python
# Base Dashboard (Abstract)
class BaseDashboard(ABC):
    dashboard_id: str
    title: str
    description: str
    
    @abstractmethod
    def get_data(context, **kwargs) -> Dict
    
    @abstractmethod
    def render(data, context) -> Response
    
    def handle_request(user_context, **kwargs):
        # 1. Check access
        # 2. Get filters
        # 3. Check cache
        # 4. Fetch data
        # 5. Cache data
        # 6. Render
```

#### Dashboard Registry Pattern

```python
# app/dashboards/registry.py
class DashboardRegistry:
    _dashboards: Dict[str, BaseDashboard]
    
    @classmethod
    def register(cls, dashboard_class):
        """Register dashboard"""
    
    @classmethod
    def get(cls, dashboard_id) -> BaseDashboard:
        """Get dashboard by ID"""
    
    @classmethod
    def get_accessible_dashboards(cls, user_context):
        """Get accessible dashboards for user"""
```

#### Data Provider Pattern

```python
# app/dashboards/data_providers/base.py
class DataProvider(ABC):
    def execute_query(query, params, context):
        """Execute query with context filters"""
    
    def _apply_context_filters(query, params, context):
        """Apply user access filters to query"""
```

#### Cache System

```python
# app/dashboards/cache.py
class DashboardCache:
    @staticmethod
    def get(key) -> Optional[Dict]:
        """Get from cache"""
    
    @staticmethod
    def set(key, value, ttl):
        """Set cache with TTL"""
    
    @staticmethod
    def generate_key(prefix, **kwargs) -> str:
        """Generate cache key"""
```

### ماژول 3: Admin Panel

#### ساختار

```
admin/
├── __init__.py          # Blueprint registration
├── routes.py            # Admin routes
├── utils.py             # Admin utilities
├── scheduler.py         # Auto-sync scheduler
├── sync_handlers.py     # Sync handlers
└── sync_progress.py     # Sync progress tracking
```

#### Routes

```python
# User Management
/admin/users              # List users
/admin/users/<id>        # User detail
/admin/users/<id>/edit   # Edit user
/admin/users/new         # Create user

# Dashboard Access
/admin/dashboard-access   # List accesses
/admin/dashboard-access/new    # Create access
/admin/dashboard-access/<id>/edit  # Edit access

# Data Sync
/admin/data-sync         # List syncs
/admin/data-sync/<id>/edit       # Edit sync
/admin/data-sync/<id>/sync      # Trigger sync
/admin/data-sync/<id>/stop      # Stop sync
/admin/data-sync/<id>/restart   # Restart sync

# Logs
/admin/logs              # Access logs
/admin/data-sync/logs    # Sync logs
```

### ماژول 4: Data Synchronization

#### Sync Types

1. **Scheduled Sync** (Faculty, Students)
   - استفاده از Scheduler
   - اجرای دوره‌ای بر اساس Interval
   - Background Thread

2. **Continuous Sync** (LMS)
   - Thread مداوم
   - Sleep بین هر Sync
   - قابلیت Stop/Start

#### Sync Handler Structure

```python
# app/admin/sync_handlers.py
def run_sync_by_source(data_source, user_id, sync_id):
    """Run sync for specific data source"""
    
def run_lms_sync(user_id, sync_id, manual_sync=False):
    """Run LMS sync (continuous or manual)"""
    
def stop_sync_by_source(data_source, sync_id):
    """Stop running sync"""
```

#### Scheduler Structure

```python
# app/admin/scheduler.py
def scheduler_loop():
    """Main scheduler loop"""
    while running:
        check_and_run_scheduled_syncs()
        sleep(60)

def check_and_run_scheduled_syncs():
    """Check for due syncs and run them"""
```

### ماژول 5: Project Management

#### Kanban Structure

```
Project
  ├── KanbanColumns (Default: 3)
  │   ├── "برای انجام" (order: 1)
  │   ├── "در دست انجام" (order: 2)
  │   └── "انجام شده" (order: 3)
  │
  └── Tasks
      ├── Assigned Users (Many-to-Many)
      ├── Labels (Many-to-Many)
      └── Reports (One-to-Many)
```

#### Models

```python
# app/models.py
class Project:
    - id
    - title
    - description
    - creator_id
    - owner_id
    - columns: List[KanbanColumn]
    - members: List[User]

class KanbanColumn:
    - id
    - project_id
    - title
    - order
    - tasks: List[Task]
    - users: List[User]

class Task:
    - id
    - column_id
    - project_id
    - title
    - description
    - assigned_users: List[User]
    - assigned_labels: List[TaskLabelAssignment]
```

---

## طراحی پایگاه داده

### ER Diagram (ساده‌شده)

```
┌──────────┐
│  Users   │
└────┬─────┘
     │
     ├───< (1:N) ───┐
     │              │
     │         ┌─────────────┐
     │         │AccessLevels │
     │         └─────────────┘
     │
     ├───< (1:N) ───┐
     │              │
     │         ┌─────────────┐
     │         │  Projects    │
     │         │  (creator)   │
     │         └─────────────┘
     │
     ├───< (1:N) ───┐
     │              │
     │         ┌─────────────┐
     │         │  Projects   │
     │         │  (owner)    │
     │         └─────────────┘
     │
     └───< (N:M) ───┐
                    │
              ┌─────────────┐
              │  Projects    │
              │  (members)  │
              └─────────────┘

┌──────────┐         ┌──────────────┐
│ Projects │────────>│KanbanColumns │
└──────────┘  1:N    └──────┬───────┘
                             │
                             │ 1:N
                             │
                        ┌─────────┐
                        │  Tasks  │
                        └─────────┘
```

### جداول اصلی

#### access_control.db

**users**
- id (PK)
- name
- email
- sso_id (UNIQUE)
- province_code
- university_code
- faculty_code

**access_levels**
- id (PK)
- user_id (FK → users.id)
- level

**projects**
- id (PK)
- title
- description
- creator_id (FK → users.id)
- owner_id (FK → users.id)
- start_date
- end_date
- updated_at

**kanban_columns**
- id (PK)
- project_id (FK → projects.id)
- title
- order

**tasks**
- id (PK)
- column_id (FK → kanban_columns.id)
- project_id (FK → projects.id)
- title
- description
- due_date
- start_date

**dashboard_access**
- id (PK)
- user_id (FK → users.id)
- dashboard_id
- can_access
- filter_restrictions (JSON)
- date_from
- date_to

**data_syncs**
- id (PK)
- data_source (UNIQUE)
- sync_type
- status
- auto_sync_enabled
- sync_interval_value
- sync_interval_unit
- api_endpoint
- api_username
- api_password
- last_sync_at
- next_sync_at
- records_synced

#### faculty_data.db

**faculty**
- code_markaz (PK)
- name
- province_code
- university_code
- ... (سایر فیلدها)

**students**
- id (PK)
- student_id (UNIQUE)
- name
- faculty_code
- ... (سایر فیلدها)

**monitor_data**
- id (PK)
- url
- timestamp
- key
- value

---

## طراحی API

### Internal API (برای Frontend)

#### Dashboard API

```python
GET /dashboards/
# Returns: List of accessible dashboards

GET /dashboards/<dashboard_id>
# Returns: Rendered dashboard

GET /api/dashboard/<dashboard_id>/data
# Returns: JSON data for dashboard
```

#### Filter API

```python
GET /api/dashboard/<dashboard_id>/filters
# Returns: Available filters based on user context

POST /api/dashboard/<dashboard_id>/filter
# Body: {province_code, university_code, faculty_code, date_from, date_to}
# Returns: Filtered data
```

#### Sync API

```python
POST /sync-lms-now
# Trigger manual LMS sync

GET /admin/data-sync/<id>/progress
# Returns: Sync progress
```

### External API Integration

#### Faculty API

```
POST /Login
Body: {userName, password}
Response: {data: {token}}

POST /Faculty
Headers: {Authorization: Bearer <token>}
Body: {pageNumber, pageSize}
Response: {data: {items: [...], totalCount}}
```

#### Students API

```
POST /Login
Body: {userName, password}
Response: {data: {token}}

POST /Students
Headers: {Authorization: Bearer <token>}
Body: {
    codePardis: string,
    term: string,
    paging: {pageNumber, pageSize},
    Filter: {}
}
Response: {data: {items: [...], totalCount}}
```

#### LMS API

```
GET https://lms1.cfu.ac.ir/mod/adobeconnect/monitor.php
Response: "online_lms_user 20 online_adobe_class 0 ..."
```

---

## طراحی امنیت

### Security Layers

```
┌─────────────────────────────────────┐
│      Network Security (HTTPS)       │
└─────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────┐
│      Authentication (SSO)           │
└─────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────┐
│      Authorization (RBAC)           │
└─────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────┐
│      Data Access Control            │
└─────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────┐
│      Input Validation               │
└─────────────────────────────────────┘
```

### Security Measures

1. **Session Security**
   - Secure Cookies
   - HttpOnly Flag
   - SameSite Protection
   - Session Timeout

2. **Access Control**
   - Role-based Access Control (RBAC)
   - Context-based Filtering
   - Dashboard Access Control

3. **Data Protection**
   - SQL Injection Prevention (ORM)
   - XSS Prevention (Template Escaping)
   - CSRF Protection (State in OAuth)

4. **Credential Management**
   - Environment Variables
   - No Hardcoded Secrets
   - Encrypted Storage (if needed)

---

## طراحی UI/UX

### Design Principles

1. **RTL Support**
   - تمام صفحات RTL
   - فونت فارسی (Vazir)
   - تقویم شمسی

2. **Responsive Design**
   - Bootstrap RTL
   - Mobile-friendly (تا حد امکان)

3. **User Experience**
   - Navigation واضح
   - پیام‌های خطای واضح
   - Loading States
   - Feedback برای Actions

### Layout Structure

```
┌─────────────────────────────────────┐
│         Navigation Bar             │
│  [Logo] [Dashboards] [Admin] [User]│
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│                                     │
│         Main Content Area           │
│                                     │
│  ┌─────────────────────────────┐   │
│  │   Dashboard Content         │   │
│  │   - Filters                 │   │
│  │   - Charts                  │   │
│  │   - Tables                  │   │
│  └─────────────────────────────┘   │
│                                     │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│            Footer                  │
└─────────────────────────────────────┘
```

### Component Library

1. **Charts**
   - Chart.js برای نمودارهای خطی
   - Plotly برای نمودارهای پیشرفته
   - Map Visualizations با GeoPandas

2. **Tables**
   - Bootstrap Tables
   - Sortable Columns
   - Pagination

3. **Forms**
   - Bootstrap Forms
   - Validation
   - Date Pickers (Jalali)

---

## Design Patterns استفاده شده

### 1. Registry Pattern

**استفاده:** Dashboard Registry

```python
class DashboardRegistry:
    _dashboards: Dict[str, BaseDashboard]
    
    @classmethod
    def register(cls, dashboard_class):
        instance = dashboard_class()
        cls._dashboards[instance.dashboard_id] = instance
        return dashboard_class
```

### 2. Template Method Pattern

**استفاده:** BaseDashboard

```python
class BaseDashboard(ABC):
    def handle_request(self, user_context, **kwargs):
        # Template method
        if not self.check_access(user_context):
            return self.render_error(...)
        data = self.get_data(user_context, **kwargs)
        return self.render(data, user_context)
    
    @abstractmethod
    def get_data(self, context, **kwargs):
        pass
    
    @abstractmethod
    def render(self, data, context):
        pass
```

### 3. Strategy Pattern

**استفاده:** Data Providers

```python
class DataProvider(ABC):
    @abstractmethod
    def get_default_db_path(self) -> str:
        pass

class FacultyDataProvider(DataProvider):
    def get_default_db_path(self) -> str:
        return DashboardConfig.FACULTY_DB
```

### 4. Factory Pattern

**استفاده:** Dashboard Creation

```python
@DashboardRegistry.register
class FacultyStatsDashboard(BaseDashboard):
    def __init__(self):
        super().__init__("d1", "آمار دانشکده‌ها")
```

### 5. Observer Pattern

**استفاده:** Event Listeners

```python
@event.listens_for(Project, 'after_insert')
def create_default_kanban_columns(mapper, connection, target):
    # Create default columns
```

### 6. Singleton Pattern

**استفاده:** Cache, Registry

```python
class DashboardCache:
    _instance = None
    
    @classmethod
    def get_instance(cls):
        if cls._instance is None:
            cls._instance = cls()
        return cls._instance
```

---

## نمودارهای UML

### Class Diagram (ساده‌شده)

```
┌──────────────────┐
│  BaseDashboard   │
│  (Abstract)      │
├──────────────────┤
│ +dashboard_id    │
│ +title           │
│ +get_data()      │
│ +render()        │
│ +handle_request()│
└────────┬─────────┘
         │
         │ inherits
         │
    ┌────┴────┐
    │         │
┌───▼───┐ ┌───▼────┐
│  D1   │ │  D2    │
│       │ │        │
└───────┘ └────────┘

┌──────────────────┐
│ DataProvider     │
│  (Abstract)      │
├──────────────────┤
│ +execute_query() │
└────────┬─────────┘
         │
    ┌────┴────┐
┌───▼───┐ ┌───▼────┐
│Faculty│ │Students│
└───────┘ └────────┘
```

### Sequence Diagram: Dashboard Request

```
User → Route → DashboardRegistry → BaseDashboard
                                    ↓
                              check_access()
                                    ↓
                              get_data()
                                    ↓
                              DataProvider
                                    ↓
                              Database
                                    ↓
                              Cache
                                    ↓
                              render()
                                    ↓
                              Template
                                    ↓
                              Response → User
```

### Sequence Diagram: Data Sync

```
Scheduler → check_and_run_scheduled_syncs()
                ↓
            DataSync.query
                ↓
            run_sync_by_source()
                ↓
            API Gateway
                ↓
            Parse Data
                ↓
            Store in DB
                ↓
            Update Status
```

---

## نتیجه‌گیری

این سیستم با معماری ماژولار و استفاده از Design Patterns مناسب طراحی شده است. جداسازی Concerns، قابلیت توسعه، و نگهداری آسان از ویژگی‌های کلیدی این طراحی است.

---

**تاریخ ایجاد**: 1404/01/15
**نسخه**: 1.0.0
**نگهدارنده**: تیم توسعه




