# سند تحلیل کلی پروژه - سیستم BI دانشگاه

## 📋 خلاصه اجرایی

این پروژه یک **سامانه هوش تجاری (Business Intelligence)** برای دانشگاه است که شامل دو پنل کاربری و یک پنل مدیریتی می‌باشد. سیستم با استفاده از Flask و معماری مدرن طراحی شده و قابلیت‌های پیشرفته‌ای برای مدیریت و تحلیل داده‌های دانشگاهی ارائه می‌دهد.

### اطلاعات کلی پروژه

- **نام پروژه**: سامانه BI دانشگاه
- **نسخه**: 1.0.0
- **زبان برنامه‌نویسی**: Python 3.x
- **فریم‌ورک اصلی**: Flask 3.1.0
- **پایگاه داده**: SQLite
- **تاریخ شروع**: 1403
- **وضعیت**: در حال توسعه

---

## 🎯 اهداف پروژه

### اهداف اصلی

1. **ارائه داشبوردهای تحلیلی**: نمایش داده‌های دانشکده‌ها، دانشجویان و سیستم LMS
2. **مدیریت دسترسی چندسطحی**: کنترل دسترسی بر اساس سطح سازمانی (مرکزی، استان، دانشکده)
3. **همگام‌سازی خودکار داده‌ها**: دریافت و به‌روزرسانی خودکار داده‌ها از API Gateway
4. **مدیریت پروژه و وظایف**: سیستم Kanban برای مدیریت پروژه‌ها و تسک‌ها
5. **امنیت و احراز هویت**: استفاده از SSO مرکزی دانشگاه

### اهداف فرعی

- ارائه گزارش‌های تحلیلی در قالب نقشه و نمودار
- پشتیبانی از تقویم شمسی (جلالی)
- رابط کاربری فارسی و RTL
- قابلیت ارسال پیامک برای هشدارها

---

## 🏗️ معماری کلی سیستم

### معماری سه‌لایه

```
┌─────────────────────────────────────┐
│      Presentation Layer             │
│  (Templates, Static Files, UI)      │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│      Application Layer              │
│  (Routes, Blueprints, Business Logic)│
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│      Data Layer                     │
│  (Models, Data Providers, Database) │
└─────────────────────────────────────┘
```

### کامپوننت‌های اصلی

1. **Authentication & Authorization**
   - SSO Integration (OAuth2)
   - Flask-Login
   - Role-based Access Control

2. **Dashboard System**
   - Base Dashboard Architecture
   - Dashboard Registry
   - Data Providers
   - Visualization Components

3. **Admin Panel**
   - User Management
   - Dashboard Access Control
   - Data Sync Management
   - Access Logging

4. **Project Management**
   - Kanban Board
   - Task Management
   - Label System

5. **Data Synchronization**
   - API Gateway Integration
   - Scheduled Sync
   - Manual Sync

---

## 📦 ماژول‌های سیستم

### 1. ماژول احراز هویت (Authentication)

**مسئولیت‌ها:**
- مدیریت ورود و خروج کاربران
- یکپارچگی با SSO مرکزی
- مدیریت Session
- کنترل دسترسی

**فایل‌های کلیدی:**
- `app/auth_utils.py`
- `app/app.py` (routes: `/login`, `/authorized`, `/logout`)

### 2. ماژول داشبوردها (Dashboards)

**مسئولیت‌ها:**
- نمایش داشبوردهای تحلیلی
- فیلترینگ داده‌ها بر اساس سطح دسترسی
- مدیریت Cache
- رندر کردن نمودارها و نقشه‌ها

**فایل‌های کلیدی:**
- `app/dashboards/`
- `app/dashboard_routes.py`

**داشبوردهای موجود:**
- داشبورد آمار دانشکده‌ها (d1)
- نقشه دانشکده‌ها (d2)
- نقشه پردیس‌ها (d3)
- نسبت دانشجو به استاد (d7)
- مانیتورینگ LMS (d8)
- داشبورد دانشجویان

### 3. ماژول پنل مدیریت (Admin Panel)

**مسئولیت‌ها:**
- مدیریت کاربران
- مدیریت دسترسی به داشبوردها
- مدیریت همگام‌سازی داده‌ها
- مشاهده لاگ‌های دسترسی

**فایل‌های کلیدی:**
- `app/admin/`
- `app/admin_models.py`

### 4. ماژول مدیریت پروژه (Project Management)

**مسئولیت‌ها:**
- مدیریت پروژه‌ها
- سیستم Kanban
- مدیریت تسک‌ها
- سیستم برچسب‌گذاری

**فایل‌های کلیدی:**
- `app/kanban.py`
- `app/tasks.py`
- `app/label_management.py`

### 5. ماژول همگام‌سازی داده‌ها (Data Sync)

**مسئولیت‌ها:**
- دریافت داده از API Gateway
- همگام‌سازی خودکار (Scheduled)
- همگام‌سازی دستی
- مدیریت وضعیت Sync

**فایل‌های کلیدی:**
- `app/admin/sync_handlers.py`
- `app/admin/scheduler.py`
- `app/fetch_data/`

**منابع داده:**
- Faculty Data (دانشکده‌ها)
- Students Data (دانشجویان)
- LMS Data (سیستم مدیریت یادگیری)

---

## 🔐 سطوح دسترسی

### 1. سازمان مرکزی (Central Organization)
- **دسترسی**: تمام داده‌های کشور
- **قابلیت فیلتر**: استان، دانشگاه، دانشکده
- **دسترسی به پنل ادمین**: خیر

### 2. دانشگاه استان (Province University)
- **دسترسی**: فقط داده‌های استان خود
- **قابلیت فیلتر**: دانشکده
- **دسترسی به پنل ادمین**: خیر

### 3. دانشکده (Faculty)
- **دسترسی**: فقط داده‌های دانشکده خود
- **قابلیت فیلتر**: ندارد
- **دسترسی به پنل ادمین**: خیر

### 4. مدیر سیستم (Admin)
- **دسترسی**: تمام داده‌ها
- **قابلیت فیلتر**: تمام فیلترها
- **دسترسی به پنل ادمین**: بله

---

## 🗄️ پایگاه داده

### دیتابیس‌های سیستم

1. **access_control.db**
   - اطلاعات کاربران
   - پروژه‌ها و تسک‌ها
   - دسترسی‌ها
   - تنظیمات همگام‌سازی

2. **faculty_data.db**
   - داده‌های دانشکده‌ها
   - داده‌های دانشجویان
   - داده‌های LMS

---

## 🛠️ تکنولوژی‌های استفاده شده

### Backend
- **Flask 3.1.0**: فریم‌ورک اصلی
- **SQLAlchemy 2.0.41**: ORM
- **Flask-Login 0.6.3**: مدیریت Session
- **Authlib 1.5.2**: OAuth2 Integration
- **Flask-Session 0.8.0**: Session Management

### Frontend
- **Jinja2**: Template Engine
- **Bootstrap RTL**: Framework UI
- **Chart.js**: نمودارها
- **Plotly**: نمودارهای پیشرفته
- **jQuery**: JavaScript Library

### Data Processing
- **Pandas 2.2.3**: پردازش داده
- **NumPy 2.2.6**: محاسبات عددی
- **GeoPandas 1.0.1**: داده‌های جغرافیایی

### Utilities
- **jdatetime 5.2.0**: تقویم شمسی
- **python-dotenv 1.1.0**: مدیریت متغیرهای محیطی
- **requests 2.32.3**: HTTP Requests

---

## 📊 ویژگی‌های کلیدی

### 1. معماری ماژولار
- جداسازی کامل ماژول‌ها
- قابلیت توسعه آسان
- استفاده از Design Patterns

### 2. سیستم Cache
- Cache برای داشبوردها
- TTL قابل تنظیم
- بهبود Performance

### 3. مدیریت خطا
- Error Handling جامع
- Logging پیشرفته
- پیام‌های خطای کاربرپسند

### 4. امنیت
- SSO Integration
- Session Security
- CSRF Protection
- SQL Injection Prevention

### 5. پشتیبانی از فارسی
- RTL Support
- تقویم شمسی
- فونت‌های فارسی

---

## 📈 وضعیت توسعه

### ✅ تکمیل شده

- [x] معماری پایه سیستم
- [x] سیستم احراز هویت SSO
- [x] معماری داشبوردها
- [x] پنل مدیریت
- [x] سیستم همگام‌سازی داده‌ها
- [x] سیستم Kanban
- [x] مدیریت دسترسی چندسطحی
- [x] Cache System
- [x] داشبوردهای اصلی

### 🔄 در حال توسعه

- [ ] تست‌های واحد (Unit Tests)
- [ ] تست‌های یکپارچگی (Integration Tests)
- [ ] مستندات API کامل
- [ ] بهینه‌سازی Performance

### 📋 برنامه‌ریزی شده

- [ ] داشبوردهای جدید
- [ ] Export به Excel/PDF
- [ ] Real-time Updates
- [ ] Mobile Responsive

---

## 📝 نکات مهم

1. **متغیرهای محیطی**: سیستم از فایل `.env` برای تنظیمات حساس استفاده می‌کند
2. **پایگاه داده**: دو دیتابیس جداگانه برای کنترل دسترسی و داده‌ها
3. **SSO**: سیستم با SSO مرکزی دانشگاه یکپارچه است
4. **Cache**: داشبوردها به صورت پیش‌فرض Cache می‌شوند
5. **Logging**: تمام عملیات مهم در فایل لاگ ثبت می‌شوند

---

## 📚 مستندات مرتبط

- [معماری سیستم](./02_SYSTEM_ARCHITECTURE.md)
- [سند طراحی](./03_DESIGN_DOCUMENT.md)
- [راهنمای توسعه](./04_DEVELOPMENT_GUIDE.md)
- [راهنمای کاربر](./07_USER_GUIDE.md)
- [راهنمای ادمین](./08_ADMIN_GUIDE.md)

---

**تاریخ آخرین به‌روزرسانی**: 1404/01/15
**نگهدارنده**: تیم توسعه
