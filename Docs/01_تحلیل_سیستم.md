# مستندات تحلیل سیستم BI

## 📋 خلاصه اجرایی

این پروژه یک سامانه **Business Intelligence (BI)** جامع برای مدیریت و تحلیل داده‌های دانشگاهی است که شامل سه پنل اصلی می‌باشد:

1. **پنل کاربری عمومی**: دسترسی به داشبوردهای تحلیلی بر اساس سطح دسترسی
2. **پنل کاربری دانشجویان**: داشبوردهای مخصوص دانشجویان
3. **پنل مدیریتی (Admin)**: مدیریت کاربران، دسترسی‌ها، همگام‌سازی داده‌ها و پیکربندی سیستم

---

## 🎯 اهداف پروژه

### اهداف اصلی:
- ارائه داشبوردهای تحلیلی برای داده‌های دانشگاهی
- مدیریت دسترسی‌های چندسطحی بر اساس موقعیت سازمانی کاربر
- همگام‌سازی خودکار داده‌ها از API Gateway
- مدیریت پروژه‌ها و وظایف با سیستم Kanban
- پشتیبانی از تقویم شمسی (جلالی) در تمام بخش‌ها

### اهداف فرعی:
- ارائه گزارش‌های تحلیلی در قالب نمودار و نقشه
- مدیریت کاربران و دسترسی‌ها
- لاگ‌گیری از فعالیت‌های کاربران
- ارسال هشدارهای SMS در شرایط خاص

---

## 🏗️ معماری کلی سیستم

### لایه‌های معماری:

```
┌─────────────────────────────────────────┐
│         Presentation Layer               │
│  (Templates, Static Files, Frontend)    │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│         Application Layer               │
│  (Flask Routes, Blueprints, Controllers) │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│         Business Logic Layer             │
│  (Dashboards, Data Providers, Services) │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│         Data Access Layer               │
│  (SQLAlchemy ORM, Database Connections) │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│         Data Storage Layer              │
│  (SQLite Databases, File System)        │
└─────────────────────────────────────────┘
```

---

## 📊 ماژول‌های اصلی سیستم

### 1. ماژول احراز هویت و مدیریت کاربران

**مسئولیت‌ها:**
- احراز هویت از طریق SSO مرکزی دانشگاه
- مدیریت نشست‌های کاربری
- تعیین سطح دسترسی کاربران
- مدیریت اطلاعات سازمانی کاربران (استان، دانشگاه، دانشکده)

**فایل‌های مرتبط:**
- `app/auth_utils.py`
- `app/app.py` (routes: `/login`, `/authorized`, `/logout`)
- `app/models.py` (User, AccessLevel)
- `app/dashboards/context.py` (UserContext)

---

### 2. ماژول داشبوردهای تحلیلی

**مسئولیت‌ها:**
- نمایش داشبوردهای تحلیلی مختلف
- فیلتر کردن داده‌ها بر اساس سطح دسترسی
- ارائه نمودارها و نقشه‌های تعاملی
- مدیریت کش برای بهبود عملکرد

**داشبوردهای موجود:**
- **d1**: آمار اعضای هیئت علمی
- **d2**: نقشه توزیع اعضای هیئت علمی
- **d3**: نقشه توزیع پردیس‌ها
- **d7**: نسبت دانشجو به استاد
- **d8**: آمار دانشجویان
- **students**: داشبورد دانشجویان
- **lms_monitoring**: مانیتورینگ سیستم LMS

**فایل‌های مرتبط:**
- `app/dashboards/base.py` (BaseDashboard)
- `app/dashboards/registry.py` (DashboardRegistry)
- `app/dashboards/dashboards/` (داشبوردهای خاص)
- `app/dashboard_routes.py`

---

### 3. ماژول مدیریت داده (Data Providers)

**مسئولیت‌ها:**
- دریافت داده‌ها از پایگاه داده
- اعمال فیلترهای دسترسی
- تبدیل داده‌ها به فرمت مناسب برای نمایش
- مدیریت اتصالات پایگاه داده

**Data Providers موجود:**
- `FacultyDataProvider`: داده‌های اعضای هیئت علمی
- `StudentsDataProvider`: داده‌های دانشجویان
- `LMSDataProvider`: داده‌های سیستم LMS
- `PardisDataProvider`: داده‌های پردیس‌ها

**فایل‌های مرتبط:**
- `app/dashboards/data_providers/base.py`
- `app/dashboards/data_providers/faculty.py`
- `app/dashboards/data_providers/students.py`
- `app/dashboards/data_providers/lms.py`

---

### 4. ماژول پنل مدیریتی (Admin Panel)

**مسئولیت‌ها:**
- مدیریت کاربران و دسترسی‌ها
- مدیریت دسترسی به داشبوردها
- مدیریت همگام‌سازی داده‌ها
- مشاهده لاگ‌های سیستم
- پیکربندی داشبوردها

**قابلیت‌های اصلی:**
- ایجاد، ویرایش و حذف کاربران
- تعیین سطح دسترسی کاربران
- تنظیم دسترسی به داشبوردهای خاص
- مدیریت همگام‌سازی خودکار داده‌ها
- مشاهده گزارش فعالیت‌های کاربران

**فایل‌های مرتبط:**
- `app/admin/routes.py`
- `app/admin/utils.py`
- `app/admin/scheduler.py`
- `app/admin/sync_handlers.py`
- `app/admin_models.py`

---

### 5. ماژول همگام‌سازی داده‌ها (Data Sync)

**مسئولیت‌ها:**
- همگام‌سازی داده‌ها از API Gateway
- مدیریت همگام‌سازی خودکار (Scheduler)
- ردیابی وضعیت همگام‌سازی
- مدیریت خطاها و بازگشت به حالت قبلی

**منابع داده:**
- **Faculty API**: داده‌های اعضای هیئت علمی
- **Students API**: داده‌های دانشجویان
- **LMS API**: داده‌های سیستم LMS

**فایل‌های مرتبط:**
- `app/admin/scheduler.py`
- `app/admin/sync_handlers.py`
- `app/admin/sync_progress.py`
- `app/fetch_data/faculty_main.py`
- `app/fetch_data/students_main.py`
- `app/fetch_data/lms_sync.py`

---

### 6. ماژول مدیریت پروژه (Kanban)

**مسئولیت‌ها:**
- مدیریت پروژه‌ها
- مدیریت وظایف (Tasks) در قالب Kanban Board
- اختصاص کاربران به ستون‌ها و وظایف
- مدیریت برچسب‌ها (Labels)

**فایل‌های مرتبط:**
- `app/kanban.py`
- `app/tasks.py`
- `app/label_management.py`
- `app/task_label_assignment.py`

---

## 🔐 سیستم دسترسی و امنیت

### سطوح دسترسی:

1. **سازمان مرکزی (CENTRAL_ORG)**
   - دسترسی به تمام داده‌های کشور
   - امکان فیلتر بر اساس استان، دانشگاه، دانشکده
   - دسترسی به تمام داشبوردها

2. **دانشگاه استان (PROVINCE_UNIVERSITY)**
   - دسترسی فقط به داده‌های استان خود
   - امکان فیلتر بر اساس دانشکده
   - دسترسی محدود به داشبوردها

3. **دانشکده (FACULTY)**
   - دسترسی فقط به داده‌های دانشکده خود
   - عدم امکان فیلتر
   - دسترسی محدود به داشبوردها

4. **مدیر سیستم (ADMIN)**
   - دسترسی کامل به تمام بخش‌ها
   - دسترسی به پنل مدیریتی
   - امکان مدیریت کاربران و تنظیمات

### مکانیزم احراز هویت:

- استفاده از **OAuth2** برای احراز هویت از طریق SSO مرکزی
- مدیریت نشست با **Flask-Session**
- استفاده از **Flask-Login** برای مدیریت کاربران
- ذخیره اطلاعات کاربر در Session

---

## 📈 جریان داده در سیستم

### 1. جریان همگام‌سازی داده:

```
API Gateway → Sync Handler → Database → Data Provider → Dashboard
```

### 2. جریان نمایش داشبورد:

```
User Request → Authentication → Authorization → Data Provider → Cache → Template → Response
```

### 3. جریان فیلتر داده:

```
User Context → Access Level → Data Filters → SQL Query → Filtered Results
```

---

## 🗄️ ساختار پایگاه داده

### پایگاه داده اصلی (`access_control.db`):

**جداول اصلی:**
- `users`: اطلاعات کاربران
- `access_levels`: سطوح دسترسی کاربران
- `projects`: پروژه‌ها
- `kanban_columns`: ستون‌های Kanban
- `tasks`: وظایف
- `labels`: برچسب‌ها
- `dashboard_access`: دسترسی به داشبوردها
- `access_logs`: لاگ فعالیت‌ها
- `data_syncs`: تنظیمات همگام‌سازی
- `dashboard_configs`: پیکربندی داشبوردها

### پایگاه داده داده‌ها (`faculty_data.db`):

**جداول اصلی:**
- `faculty`: اطلاعات اعضای هیئت علمی
- `students`: اطلاعات دانشجویان
- `monitor_data`: داده‌های مانیتورینگ LMS
- `pardis`: اطلاعات پردیس‌ها

---

## 🛠️ تکنولوژی‌های استفاده شده

### Backend:
- **Flask 3.1.0**: فریمورک اصلی وب
- **SQLAlchemy 2.0.41**: ORM برای دسترسی به پایگاه داده
- **Flask-Login**: مدیریت احراز هویت
- **Authlib**: پیاده‌سازی OAuth2
- **Flask-Session**: مدیریت نشست‌ها

### Frontend:
- **Jinja2**: موتور قالب‌ها
- **Bootstrap 5 (RTL)**: فریمورک CSS
- **Chart.js**: نمودارهای تعاملی
- **Plotly**: نمودارهای پیشرفته
- **jQuery**: کتابخانه JavaScript

### پایگاه داده:
- **SQLite**: پایگاه داده اصلی
- دو پایگاه داده جداگانه برای کنترل دسترسی و داده‌ها

### سایر:
- **jdatetime**: پشتیبانی از تقویم شمسی
- **Waitress**: سرور WSGI برای Production
- **python-dotenv**: مدیریت متغیرهای محیطی

---

## 📝 خلاصه تحلیل

### نقاط قوت:
1. ✅ معماری ماژولار و قابل توسعه
2. ✅ سیستم دسترسی چندسطحی قوی
3. ✅ پشتیبانی از کش برای بهبود عملکرد
4. ✅ همگام‌سازی خودکار داده‌ها
5. ✅ پشتیبانی از تقویم شمسی
6. ✅ رابط کاربری RTL و فارسی

### نقاط قابل بهبود:
1. ⚠️ استفاده از SQLite (محدودیت در مقیاس‌پذیری)
2. ⚠️ عدم استفاده از Redis برای کش (استفاده از حافظه)
3. ⚠️ نیاز به بهبود مدیریت خطاها
4. ⚠️ نیاز به تست‌های واحد بیشتر
5. ⚠️ نیاز به مستندسازی API

---

## 🎯 نتیجه‌گیری

این سیستم یک پلتفرم BI جامع و قابل توسعه است که با معماری ماژولار و سیستم دسترسی چندسطحی، امکان ارائه گزارش‌های تحلیلی را برای سطوح مختلف سازمانی فراهم می‌کند. سیستم با استفاده از تکنولوژی‌های مدرن و استاندارد، قابلیت توسعه و نگهداری بالایی دارد.



