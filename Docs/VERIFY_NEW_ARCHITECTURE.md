# راهنمای بررسی استفاده از معماری جدید

## ✅ صفحه لیست داشبوردها

بله، صفحه‌ای که می‌بینید همان صفحه لیست داشبوردها است. اما باید بررسی کنید که آیا از معماری جدید استفاده می‌کند یا نه.

---

## 🔍 نحوه بررسی

### 1. بررسی در Developer Tools (F12)

1. **Console Tab:**
   - خطاهای JavaScript را بررسی کنید
   - اگر خطایی وجود دارد، ممکن است از معماری قدیمی استفاده شود

2. **Network Tab:**
   - روی یکی از داشبوردها کلیک کنید
   - بررسی کنید که request به `/dashboards/d1` انجام می‌شود
   - Response را بررسی کنید

### 2. بررسی در مرورگر

#### تست 1: کلیک روی داشبورد
1. روی "آمار کلی اعضای هیات علمی" کلیک کنید
2. URL باید یکی از این‌ها باشد:
   - `/dashboards/d1` (جدید)
   - `/dashboards/d1?province_code=1` (با فیلتر)

#### تست 2: بررسی فیلترها
- اگر کاربر مرکزی هستید، باید فیلترها نمایش داده شوند
- اگر کاربر استان یا دانشکده هستید، نباید فیلترها نمایش داده شوند

#### تست 3: بررسی داده‌ها
- داده‌ها باید بر اساس سطح دسترسی شما فیلتر شده باشند
- کاربر مرکزی: همه داده‌ها
- کاربر استان: فقط استان خود
- کاربر دانشکده: فقط دانشکده خود

---

## 🐛 اگر از معماری قدیمی استفاده می‌شود

### مشکل: Route قدیمی استفاده می‌شود

**راه‌حل:** بررسی کنید که blueprint جدید ثبت شده است:

```python
# در app.py باید این خطوط وجود داشته باشند:
from dashboard_routes import dashboard_bp as new_dashboard_bp
app.register_blueprint(new_dashboard_bp)
```

### مشکل: فیلترها نمایش داده نمی‌شوند

**راه‌حل:** 
1. بررسی کنید که `user_context` به template پاس داده می‌شود
2. بررسی کنید که template `_filters.html` include شده است
3. بررسی کنید که سطح دسترسی کاربر درست است

### مشکل: داده‌ها فیلتر نمی‌شوند

**راه‌حل:**
1. بررسی کنید که `UserContext` به درستی ایجاد می‌شود
2. بررسی کنید که `province_code`, `faculty_code` در دیتابیس تنظیم شده
3. بررسی کنید که Data Provider از context استفاده می‌کند

---

## ✅ چک‌لیست بررسی

### صفحه لیست داشبوردها:
- [ ] صفحه لیست داشبوردها نمایش داده می‌شود
- [ ] لیست داشبوردها کامل است
- [ ] جستجو کار می‌کند

### داشبوردها:
- [ ] روی داشبورد کلیک می‌کنید و داده نمایش داده می‌شود
- [ ] URL درست است (`/dashboards/d1`)
- [ ] خطای JavaScript وجود ندارد

### فیلترها (برای کاربران مرکزی):
- [ ] فیلتر استان نمایش داده می‌شود
- [ ] فیلتر دانشکده نمایش داده می‌شود
- [ ] فیلترها کار می‌کنند

### دسترسی:
- [ ] کاربران مرکزی همه داده‌ها را می‌بینند
- [ ] کاربران استان فقط داده‌های استان خود را می‌بینند
- [ ] کاربران دانشکده فقط داده‌های دانشکده خود را می‌بینند

---

## 🔧 تست سریع

### تست 1: بررسی Route
در Developer Tools > Network:
1. روی داشبورد کلیک کنید
2. بررسی کنید که request به `/dashboards/d1` انجام می‌شود
3. Response status باید 200 باشد

### تست 2: بررسی فیلترها
1. اگر کاربر مرکزی هستید
2. به داشبورد بروید
3. باید فیلترها را ببینید

### تست 3: بررسی داده‌ها
1. به داشبورد بروید
2. داده‌ها را بررسی کنید
3. باید بر اساس سطح دسترسی فیلتر شده باشند

---

## 📝 نتیجه

اگر همه موارد بالا درست هستند، یعنی از معماری جدید استفاده می‌شود! 🎉

اگر مشکلی وجود دارد، لاگ‌های سرور را بررسی کنید یا به من اطلاع دهید.


