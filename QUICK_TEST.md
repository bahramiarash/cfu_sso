# راهنمای سریع تست

## 🚀 تست سریع (5 دقیقه)

### 1. تست ساختار
```bash
python run_tests.py
```

### 2. تست دسترسی کاربران
```bash
python test_user_access.py
```

### 3. تست از مرورگر
1. سرور را اجرا کنید:
```bash
cd app
python app.py
```

2. به آدرس بروید:
```
http://localhost:5000/dashboards
```

3. با یکی از کاربران تست وارد شوید:
   - `test_central` - کاربر مرکزی
   - `test_province` - کاربر استان
   - `test_faculty` - کاربر دانشکده

### 4. تست Unit Tests
```bash
python -m pytest tests/test_dashboards.py -v
```

---

## 📋 چک‌لیست سریع

- [ ] `python run_tests.py` بدون خطا اجرا می‌شود
- [ ] `python test_user_access.py` کاربران را ایجاد می‌کند
- [ ] داشبوردها در مرورگر نمایش داده می‌شوند
- [ ] فیلترها کار می‌کنند (برای کاربران مرکزی)

---

## 🔍 تست دستی

### تست داشبورد d1:
```
http://localhost:5000/dashboards/d1
```

### تست با فیلتر:
```
http://localhost:5000/dashboards/d1?province_code=1
```

### تست API:
```
http://localhost:5000/api/dashboards/provinces
http://localhost:5000/api/dashboards/faculties
```

---

## ⚠️ مشکلات رایج

### مشکل: "No module named 'app'"
**راه‌حل:** از root directory اجرا کنید:
```bash
cd C:\services\cert2
python run_tests.py
```

### مشکل: "User not found"
**راه‌حل:** ابتدا `test_user_access.py` را اجرا کنید تا کاربران ایجاد شوند.

### مشکل: "Database locked"
**راه‌حل:** مطمئن شوید که سرور Flask در حال اجرا نیست.

---

برای راهنمای کامل، به `Docs/TESTING_GUIDE.md` مراجعه کنید.


